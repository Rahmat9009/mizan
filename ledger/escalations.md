# Escalations ledger (append-only) — THE HUMAN READS THIS FIRST

Format: `[UTC timestamp] severity | lane | what | why it needs a human | what the Orchestrator did meanwhile`

- [2026-09-02T14:32:10Z] INFO | ORCH | Docker Desktop daemon is not running on this machine | Postgres per-tenant schema isolation (B3) is authored as SQL + docker-compose but cannot be executed or tested in this run; SQLite one-file-per-tenant ledger is the tested isolation for now | Continued; L0 ships the compose base and init SQL untested and says so in progress.md.
- [2026-09-02T14:32:10Z] INFO | ORCH | TradingAgents licence is unverified (Master Plan §14) | A human must confirm the licence before the W8 adapter ships in a release | L3 builds the adapter against a local stub; it is not marked shippable.
- [2026-09-02T14:41:09Z] INFO | ORCH | The "Killer Feature Verdict" document arrived truncated (cut inside §7 "The demo, revised"; the 50k-char message limit). §1–§6 were received and applied | The revised demo script and any later sections are unknown to this run | Saved the received portion as docs/MIZAN-KILLER-FEATURE-VERDICT.md; killer demo continues to follow Master Plan §11. Please drop the full file into docs/.
- [2026-09-02T14:41:09Z] DECISION | ORCH | Risk Canon P0 items (path-dependence state, aggregate multi-agent layer) are incorporated BEFORE the contract freeze by making them RiskContext inputs (engine stays a pure function). Recorded as ADR-0006 and docs/API-SURFACE-ADDENDUM-1.md | A human may prefer a stateful engine object instead; this run chose the pure-function form because it is the only one that keeps A1 (byte-identical replay) provable | Continued.
- [2026-09-02T23:25:21Z] ESC-1 RESOLVED | ORCH | A FROZEN file (tests/invariants/test_01) was edited. Recording it because the freeze exists so this never happens silently. | The hypothesis strategy generated `advised=0` and passed it to a helper that builds an AdvisoryOpinion with recommendation=REDUCE. The contract refuses that combination — "advisory REDUCE requires a positive recommended_quantity" — because a REDUCE to zero is semantically a REJECT. So the generator produced an input the contract forbids: a defect in the test's own strategy, not in any implementation. The alternative readings were both worse: relaxing the contract to allow REDUCE-to-zero would create two ways to express one outcome and weaken E1's clarity, and leaving the suite BLOCKING would halt every lane's merges over a generator bound. | Changed `min_value=0` to `min_value=1` and documented why in the test docstring. The assertion is untouched and exactly as strong: no advised quantity, however large, lifts the authorized quantity above the deterministic cap. The advised-zero case remains covered by the REJECT path in invariant 02 and by the ValidationError cases in the same file. Reviewed and approved by the Orchestrator per §3 (a contract/invariant change requires escalation, not silence).
- [2026-09-02T23:31:56Z] BLOCKER | L0 (infra) | The CI `secret-scan` job is red: `tests/audit/test_redaction.py:24` commits a module-level `sk-` prefixed fixture literal, which `scripts/secret_scan.py --all` correctly reports. Nothing merges until it carries an inline `# secret-scan: allow` marker. | The file belongs to L2b and L0 will not edit another lane's paths, will not create a repo-root allow-list that exempts it, and will not relax the scanner's placeholder rules to accommodate it - all three hide the finding rather than resolve it. A human or the Orchestrator should route the one-line fix. | Raised as REQ-5 and reported in L0's handover. L0's tests/infra suite asserts the same condition CI asserts, so the red is visible rather than latent; scanner self-test and --history are clean.
- [2026-09-02T23:31:56Z] BLOCKER | L0 (infra) | The CI `lint` job cannot pass and no lane may fix it: ~250 of 313 ruff findings are in FROZEN or read-only paths (mizan/contracts, tests/invariants, tests/fixtures, tests/security). 224 are E501 and the longest line is 186 characters, so no line-length setting clears them. | Every available remedy is above this lane's authority: sweeping the frozen paths edits frozen files, excluding them from ruff permanently narrows the lint gate, and marking the job non-blocking weakens a CI gate. The Orchestrator must choose. | Raised as REQ-6. L0 fixed its own 9 findings in scripts/ (all E501, wrapping only, no behaviour change) and ships tests/infra clean; nothing else was touched.
- [2026-09-02T23:32:43Z] DECISION | L2a (governor/advisory/authorization) | docs/API-SURFACE.md §3.3 says the governor should add ADVISORY_UNAVAILABLE to GovernorDecision.reason_codes as an informational code when the advisory is absent/unavailable and policy.advisory.enabled is true. It does NOT do that, and cannot. | Invariant 18(c) (FROZEN) records a decision made with an advisory CONCUR, then asserts that replay(record, advisory=None) reproduces the recorded verdict_hash. verdict_hash covers reason_codes, so an informational code emitted for advisory=None but not for CONCUR makes those two hashes differ and INV-18 fails. Invariant 13 independently requires govern(advisory=None) and govern(unavailable opinion) to produce the same verdict_hash, so the code cannot be emitted for one and not the other either. The three positions are mutually exclusive; the invariants win. A human should ratify the doc change. | Implemented the invariants: ADVISORY_UNAVAILABLE appears ONLY on the fail-closed REJECT path (policy.advisory.enabled AND fail_closed.on_advisory_unavailable), which INV-13 requires. The 'the advisory was not consulted' fact is still in the record: GovernorDecision.llm_advisory carries invoked/available/profile and the reason the opinion was unusable, which is what the console renders. Suggested doc amendment: strike the parenthetical from §3.3 and say instead that unavailability is recorded on llm_advisory, never in reason_codes, except when it changes the verdict.
- [2026-09-03T00:22:00Z] DECISION | ORCH (resolving REQ-6 from L0) | The `lint` CI job could not pass and no lane was permitted to fix it: ~250 of 295 ruff findings sat in FROZEN or read-only paths (mizan/contracts, tests/invariants, tests/fixtures, tests/security), 215 of them E501, with a 186-character longest line that no line-length setting clears. L0 correctly refused all three obvious remedies and escalated. | A gate that cannot be satisfied is not a gate — it trains everyone to ignore a red job, which is worse than not having it. But the two easy fixes each destroy something: sweeping the frozen paths means a large mechanical reformat of reviewed, frozen files, which is precisely the diff a real change hides in (the reason the freeze exists), and `extend-exclude` means those paths are never linted again, including for genuine defects. | RESOLVED with the narrow option, now in pyproject.toml: `per-file-ignores` on the four frozen paths listing ONLY the cosmetic codes — E501 (line length), I001 (import order) and the UP modernisation rules. `F` (undefined and unused names) and `B` (likely bugs) stay fully enforced in those paths, and every non-frozen path keeps the complete rule set. `ruff check .` now reports 27 findings, all auto-fixable, all inside the L3/L4 stub files being rewritten this sprint, and each of those lanes has "ruff clean on your own paths" in its DoD. To revisit when the freeze lifts at the next schema version. This is a consequence of the freeze, not a lowering of the standard.
- [2026-09-03T00:23:00Z] INFO | ORCH | REQ-5 (the secret-scan BLOCKER L0 raised) is closed and needed no Orchestrator routing: L2b added the inline `# secret-scan: allow` marker to its own fixture line, which was the remedy L0 recommended and deliberately declined to apply itself. `secret_scan.py --all` is clean across 213 files, `--history` and `--self-test` clean. | No human action needed; recorded because the BLOCKER was recorded. | Continued.
- [2026-09-03T00:24:00Z] DECISION | ORCH | Granted one agent joint write access across a lane boundary (mizan/contracts/canonical.py, which is L0's, plus mizan/audit and tests/audit, which are L2b's) to close REQ-2 and REQ-3. | The 24 gap tests in tests/audit/test_redaction.py are `xfail(strict=True)`, so an xfail that starts passing FAILS the suite. Closing a redaction gap in L0's `redact()` therefore turns L2b's suite red in the same instant, and neither lane can land a green change alone. Splitting the work across two agents guarantees a red checkpoint; the §5.2 ownership rule exists to prevent collisions, and here it would cause one. The Orchestrator is the router for cross-lane requests (§5.2), so this is a routing decision, not an exception to the rule. | Authorization is scoped to REQ-2 and REQ-3 only and stated as such in the brief. `contracts/*.schema.json`, `contracts/reason_codes.json`, `contracts/error_codes.json` and `tests/invariants/` remain frozen and off-limits; "regenerating schemas produces zero diff, proven by an empty `git status --porcelain contracts/`" is an explicit DoD item.
- [2026-09-02T23:51:59Z] ESC-2 RESOLVED | L2a → ORCH | API-SURFACE §3.3 required adding ADVISORY_UNAVAILABLE as an informational reason code whenever the advisory was enabled but absent. L2a reported it unimplementable and was right. | verdict_hash covers reason_codes, so an absent advisory would hash differently from a CONCUR advisory that produced the same outcome — breaking invariant 18(c) (a recorded CONCUR must replay identically with advisory=None) and invariant 13. The invariants describe the product guarantee; the doc line described a nicety. | Struck the clause from docs/API-SURFACE.md §3.3 and recorded the general rule it taught: REASON CODES DESCRIBE THE OUTCOME, NEVER THE CIRCUMSTANCES THAT PRODUCED IT. Circumstances belong on GovernorDecision.llm_advisory, which is recorded but not hashed into the verdict. Unavailability is still fully auditable there.

- [2026-09-03T00:32:00Z] BLOCKER (worked around) | L3a (execution gate + adapters) | **Two agents were writing lane L3a's paths at the same time.** Throughout this run, `mizan/execution/`, `mizan/adapters/`, `tests/execution/` and `tests/adapters/` were being created and rewritten by another process concurrently with mine — file mtimes advanced between consecutive tool calls, and three of my edits returned "the file changed on disk since you last read it". `ledger/progress.md` records exactly one L3a dispatch, so this is a duplicate/parallel run of the same brief rather than a lane-boundary violation. | Real damage occurred before I detected it: I overwrote a `tests/execution/conftest.py` the other run had just written, its test modules were then re-pointed at MY fixture names, I restored ITS fixture names, and both test files went red — a two-cycle thrash on one shared file that cost several minutes and could have been committed red. The general hazard is worse than the specific one: two writers on one file produce a plausible-looking interleaving that no test catches, which is precisely the failure mode the §5.2 one-lane-one-owner rule exists to prevent. | Recovered by converging rather than fighting: both runs independently moved every test module to a **self-contained** form (each builds its own chain, gate and doubles; no shared fixtures), after which the shared `tests/execution/conftest.py` was dead code and I deleted it. From that point the two runs could only collide at whole-file granularity, which is recoverable. Final state is green and stable across repeated runs — invariants 18/18 PASS BLOCKING=0, tests/execution + tests/adapters 169 passed, ruff clean, secret scan clean. **Ask of the Orchestrator:** confirm only one L3a result is merged, and diff the two runs' outputs before the checkpoint if a second report exists; and gate future waves so one lane is dispatched once.
- [2026-09-03T05:45:00Z] BLOCKER-AVOIDED | L3b (sdk/api) | **Two agents were running as lane L3b in the same working tree at the same time.** This session found `mizan/sdk/__init__.py`, `mizan/api/__init__.py`, `mizan/api/auth.py` and `mizan/api/ratelimit.py` already written and being rewritten — file mtimes moving seconds apart while this agent was reading them — and later `tests/sdk/test_pipeline.py` and `tests/api/test_auth_and_isolation.py` appeared from the other agent mid-run. Nothing was lost, but only because this session changed method on discovering it. | A human should decide whether the duplicate dispatch was intended (a retry after a presumed-dead agent) and, if not, how lane dispatch is de-duplicated. The failure mode is not hypothetical: two agents doing whole-file `Write` on one module destroy each other's work silently, and the loser's tests keep passing against the winner's code, so the loss is invisible at the checkpoint. | This session adopted a **no-clobber protocol** for the rest of its run and recommends it as the standing rule for any shared tree: create NEW files with `Write`; change EXISTING files only with exact-match `Edit` or an explicit in-place patch, which fails loudly on a concurrent rewrite instead of overwriting it; re-check mtimes before each edit to a contested file. Every change this session made to the other agent's files is additive or a bug fix, and both agents' test files now coexist and pass together (260 tests across tests/sdk + tests/api). The other agent's implementation was kept as the base and NOT overwritten.
- [2026-09-03T05:45:00Z] DECISION | L3b (sdk/api) | `@mizan.protected` now hands the wrapped function the **authorized** proposal rather than the one the agent proposed, and refuses outright when `ExecutionConfig(dry_run=False)`. | Recording it because both were behaviour changes to code another agent had already written, and both were defects rather than preferences. (1) The decorator previously called `fn(proposal, ...)` with the ORIGINAL proposal after the gate approved. On a REDUCE verdict — governor authorizes 4 of the 10 shares asked for — the developer's own `broker.submit(order)` would have submitted 10. A reduction that is only reported is not a reduction; the object handed to the caller now carries the authorized leg quantities. (2) With `dry_run=False` the gate submits through Mizan's own broker AND the decorator then called the caller's submit function, placing a second order for the same authorization. That combination is now a `ConfigurationError` instead of a documented caveat. | Both are pinned: tests/sdk/test_protected_decorator.py::test_the_function_receives_the_authorized_quantities_not_the_proposed_ones and ::test_protected_refuses_a_configuration_that_would_double_submit. Invariants unaffected (18/18 PASS).
- [2026-09-03T05:45:00Z] INFO | L3b (sdk/api) | `mizan.api` had a latent defect that made every route taking the request object unusable: FastAPI was imported inside `create_app`, but the module uses `from __future__ import annotations`, so FastAPI resolved `request: Request` against the module globals, found nothing, and reinterpreted it as a required **query parameter** — `POST /v1/proposals/evaluate`, `POST /v1/control/kill-switch` and `GET /v1/health` all answered 422 before their first line ran. | Recorded because of how it hides: the package imports, the app builds, every route registers, and nothing complains until a real request arrives. Any lane using `from __future__ import annotations` in a module with framework-resolved annotations has the same trap. | Fixed by moving the import to module scope behind a guard (`FASTAPI_AVAILABLE`), so `create_app` still degrades with a clear `ConfigurationError` when the transport extra is absent while the framework-free parts — token store, rate limiter, hardening middlewares, `ROUTES` — keep working. Pinned at the signature level by tests/api/test_route_signatures.py, which asserts no route's request or principal parameter was demoted to a query param.
- [2026-09-03T01:05:00Z] VIOLATION (recorded, not reverted) | ORCH | A FROZEN file was edited with no escalation: `tests/invariants/test_10_hash_chain_verifies.py` gained `strict=False` on a `zip(...)` plus a two-line comment. It arrived inside the Orchestrator's own S2 checkpoint commit 48969ed, having come up from an S2 lane's working tree, so the Orchestrator committed a frozen-path edit without noticing. | The change is semantically null — `strict=False` is `zip`'s default, so the pairwise walk and both assertions are byte-for-byte equivalent in behaviour — and no invariant was weakened. It is recorded anyway, because the freeze exists precisely so that "harmless" frozen-path edits cannot accumulate unremarked, and because the ROOT CAUSE is an Orchestrator decision, not a lane's carelessness: the REQ-6 resolution kept ruff's `B` (likely-bug) rules enforced on the frozen paths, `B905 zip-without-explicit-strict` therefore fired inside tests/invariants, and a lane did the obvious thing and fixed the lint it was shown. A gate that reports a fixable finding in a file nobody may edit will keep producing exactly this. | Left in place: reverting would be a second unescalated edit to the same frozen file for no behavioural gain. REQ-6 amended — `B905` is added to the frozen-path per-file-ignores so the gate stops soliciting edits it forbids; `B` otherwise stays enforced. Lesson for ledger/learnings.md: when exempting frozen paths from a gate, exempt every rule the gate can actually raise there, not only the cosmetic ones.
- [2026-09-03T01:10:00Z] HALT-CLASS | ORCH | **A SECOND ORCHESTRATOR SESSION IS BUILDING THIS REPO CONCURRENTLY, IN THE SAME WORKING TREE.** Peer session `mizan-04` (interactive, started ~9h ago) is the session that produced the entire commit history under the identity `Mizan Orchestrator <orchestrator@mizan.local>` (c549b2a … f34cbd7). It is still active: it dispatched its own Sprint 3 lanes onto the same paths as this session's, and it committed `8cdb610` mid-sprint. Two of this session's agents independently reported a "duplicate agent" rewriting their files — those were its agents, not a dispatch fault here. | This is the §5.2 one-writer-per-path rule broken at SESSION level, where no lane discipline can reach it. Concrete damage already observed and recovered: a `tests/execution/conftest.py` written by one run was overwritten by the other, its tests re-pointed at the wrong fixtures, and both files went red before the two runs independently converged on self-contained test modules. `8cdb610` is worse in kind — it was made with `git add -A` while four lanes were mid-write, so it committed a partial cross-lane snapshot that passed through no checkpoint gate. The next such commit may not land green, and two writers on one file can produce a plausible interleaving that no test catches. | This session STOPPED dispatching new work. Its two in-flight agents (L3b SDK/API, L4 console) were left to finish rather than killed, because killing a mid-write agent leaves half-written files, which is the worse failure. No history was rewritten and `8cdb610` was left in place (Hard Rule: never rewrite history). Escalated to the human: only one orchestrator session may hold this working tree, and a human must choose which. Full state at the moment of detection: invariants 18/18 PASS BLOCKING=0, full suite 1926 passed / 2 failed, frozen schemas verified identical (`generate_schemas.py --check`: 9 up to date).
- [2026-09-03T06:05:00Z] BLOCKER (worked around) | L3 (execution/adapters/sdk/api) | **A third concurrent writer in lane L3's paths.** This session repeatedly found its own files rewritten mid-run: `tests/execution/conftest.py` was replaced twice by a different fixture design while this session's tests were being written against it, and `mizan/api/__init__.py` gained a `mizan/api/hardening.py` import from another writer between two of this session's reads. Nothing was lost and nothing was reverted - the peer's work is good and, where it improved on this session's (`Mizan.protected` now refuses the double-submit combination outright), it was kept. The workaround adopted here, and recommended to any lane sharing a tree: **make every test module self-contained** - build its own chain, its own gate and its own app at module level and take no fixture from a shared conftest. A test suite that depends on a conftest another agent is editing cannot be trusted to be asserting what it appears to assert. Already recorded from the other side as the L3a and L3b entries above and as ORCH's HALT-CLASS note; this entry adds only the mitigation. | Continue: full suite green (435 L3 tests, 18/18 invariants PASS, BLOCKING=0).
- [2026-09-03T01:40:00Z] ESC-3 RESOLVED | ORCH | **Concurrent orchestrators; blind commit; checkpoint gate bypassed.** (Filed as ESC-3, not ESC-2 as instructed: ESC-2 was already taken by L2a's resolved ADVISORY_UNAVAILABLE escalation earlier this sprint, and duplicate ids in an append-only ledger destroy the thing the ledger is for. Same content, unique id.) Two Claude Code sessions orchestrated this build in ONE working tree: `mizan-04` (which produced the whole history under `Mizan Orchestrator <orchestrator@mizan.local>`) and `mizan-90` (this session). Neither could see the other's dispatch. mizan-04 dispatched a single combined L3 covering execution+adapters+sdk+api at ~05:00; this session had already dispatched L3a and L3b over the same paths. Three lane agents on one side and two on the other independently reported "a duplicate agent is rewriting my files". | **The checkpoint gate was bypassed, and it mattered.** mizan-04 committed `8cdb610` and `e16004a` with `git add -A` while four of this session's lanes were mid-write, so each commit captured a partial cross-lane snapshot that no gate had run against. This was not theoretical: a clean clone of `8cdb610` is **RED - 24 failures**, all `tests/audit/test_redaction.py::test_gap_*`. That commit captured this session's L0/L2b agent's test file, in which 24 `xfail(strict=True)` gap tests had been flipped to real assertions, WITHOUT the `canonical.py` redaction fix that satisfies them. A green tree and a red commit, from the same minutes. Separately, a `tests/execution/conftest.py` was overwritten in both directions and both copies went red before the two runs independently converged on self-contained test modules. | RESOLVED. This session asked mizan-04 to stand down; it acknowledged, confirmed nothing of its own was mid-write, and handed over a full inventory naming exactly which files it authored and which it had swept but could not vouch for. Recording the stand-down here because it declined to write to the tree itself, correctly. **No history was rewritten** - `8cdb610` and `e16004a` both stand, red commit included, because rewriting is a Hard Rule violation and because a red commit that is documented is safer than a clean history that lies. `e16004a` healed the sweep: HEAD verified in a fresh clone at 1929 passed / 0 failed / 41 xfailed, invariants 18/18 PASS BLOCKING=0. Every swept path was verified COMPLETE; none needed reverting. Rule for the rest of this build: one orchestrator session per working tree, and a checkpoint commit is `git add` of a named path set, never `-A`.
- [2026-09-03T07:10:00Z] AUDIT (freeze) | L6 | **Freeze audit of `contracts/` and `tests/invariants/` at 9eebf24..HEAD: complete, four hunks, all accounted for, none weakening.** `git diff 9eebf24 HEAD -- contracts/ tests/invariants/` reports exactly four changed paths across two commits, and no file was added to or removed from either frozen tree on disk, in HEAD, or as an untracked file (`git ls-files --others` on both paths is empty; the HEAD and 9eebf24 file lists are identical but for the two new docs below). No `*.schema.json`, no `reason_codes.json`, no `error_codes.json` byte changed. Per hunk: **(1) `contracts/CANONICAL.md`, NEW, 233 lines, commit a5aab5b** — UNRECORDED at the time as a frozen-path change, recorded now. Behaviour-preserving: it is documentation, adds no schema, and its two checkable claims were verified against the running code by `tests/integration/test_evidence_pack.py::test_the_worked_examples_in_canonical_md_are_still_true` (the §1 canonical-JSON digest `41e8831…4577b` and the §2 decimal-normalisation identity both hold) and by `::test_the_exported_chain_verifies_with_the_algorithm_the_contract_publishes`, which runs §6's standalone verifier — transcribed, importing only `hashlib` and `json` — against a chain the real pipeline produced, and against a tampered, a deleted and a reordered copy of it. **(2) `contracts/README.md`, NEW, 76 lines, commit a5aab5b** — same class, same verdict: documentation, no contract content. Both were announced in `ledger/progress.md` at 2026-09-02T23:05:43Z as an L0-gap task, so they were not covert; they were simply never escalated as frozen-path writes, which the freeze rule requires. **(3) `tests/invariants/test_01`, ESC-1, hypothesis bound `min_value=0`→`1`** — known, documented, and now VERIFIED rather than accepted: `AdvisoryOpinion(recommendation="REDUCE", recommended_quantity="0")` raises `ValidationError("advisory REDUCE requires a positive recommended_quantity")`, so the removed inputs were unconstructible and the generator could never have reached the assertion with them. The assertion is byte-identical. The removed region is independently covered by `tests/governor/test_govern.py::test_authorized_never_exceeds_the_deterministic_cap_for_any_opinion`, whose strategy includes `cap=0` and the REJECT/CONCUR/UNAVAILABLE/None opinions. Behaviour-preserving; not a weakening. **(4) `tests/invariants/test_10`, `zip(..., strict=False)` + two comment lines** — known, documented at [2026-09-03T01:05:00Z]. Verified semantically null: `strict=False` is `zip`'s default and `list(zip(a, a[1:])) == list(zip(a, a[1:], strict=False))`. Behaviour-preserving. | **Nothing for a human to decide, but two things to note.** First, the freeze rule as written ("changes need a HALT entry plus Orchestrator approval") was breached four times and escalated twice; the two misses were both ADDITIONS of documentation to `contracts/`, which is the case the rule's authors probably did not have in mind and which the process therefore does not catch. If `contracts/` is frozen as a *directory*, adding a file to it is a frozen-path write and needs the same entry; if only the machine-readable artefacts are frozen, the rule should say so. Second, `contracts/CANONICAL.md` is now load-bearing — it is the specification a customer reimplements under Hard Rule A5 — so it should be listed among the artefacts a schema-version bump must re-verify. | L6 wrote no frozen file and proposes none. The verification tests live in `tests/integration/test_evidence_pack.py`, which is L6's own path, so CANONICAL.md's published claims now fail a test if the code drifts from them.
- [2026-09-03T07:10:00Z] ESC-4 | L6 → L1 (risk) + L2b (audit) + ORCH | **`duplicate_order` is enabled, configured `blocking`, reports `passed=True`, and is structurally incapable of failing in the shipped pipeline. Two identical orders one second apart both reach the paper venue.** `mizan/risk/checks.py:483` iterates `context.recent_orders`; `mizan/adapters/context.py:91` takes `recent_orders` with a default of `()`; `mizan/sdk/__init__.py:127` never supplies it, because execution outcomes never reach the chain to be derived from (REQ-12, REQ-17). The list is therefore ALWAYS empty and the check always returns `ok(...)`. The quantity arm of `erroneous_order` (`mizan/risk/checks.py:458`) reads the same list and is dead for the same reason; its price arm is alive and works. Reproduced end to end and pinned in `tests/integration/test_controls_that_cannot_fire.py`: with `duplicate_order: {enabled: true, severity: blocking, window_seconds: 60}`, two proposals differing only in `created_at` — same symbol, same side, same quantity, one second apart — are both APPROVED with empty reason codes and both SUBMITTED. The gate's idempotency step does not catch it either: `idempotency_key` is derived from `proposal_id`, and two proposals a second apart are two different proposals. | **This needs a human because the honest fix is a design choice between two things this build has deliberately not done, and because the failure mode is the one the whole product exists to prevent: a control that reports success on no evidence, inside a record whose only purpose is to be believed later.** The engine is careful about exactly this everywhere else — `aggregate_exposure` with no `aggregate_state` returns `missing(..., AGGREGATE_STATE_MISSING)` and blocks; a deferred or disabled check records `severity="info"` with a detail saying which (REQ-10 item 3). So a reader has been correctly taught that `passed=True, severity="blocking"` means a control ran and held, and here it does not. Two candidate resolutions, and L6 has no authority to pick: **(a) fail closed** — treat an absent order history as missing state (`missing(..., RECENT_ORDERS_MISSING)` or equivalent), which is E2-consistent and makes every enabled `duplicate_order` policy blocking until REQ-17 lands, i.e. it stops the shipped pipeline dead; or **(b) report honestly** — record `severity="info"` with `detail="no recent-order history is available to this build"`, which keeps the pipeline running and stops the record from claiming a control it did not perform. (b) is the smaller change and is consistent with how deferred checks are already reported; (a) is the stricter reading of E2. Either way the underlying fix is REQ-17: an order history the ledger can derive. | L6 wrote no code outside its own paths. The behaviour is pinned as it ships in `tests/integration/test_controls_that_cannot_fire.py` (4 tests), each written so that it FAILS when the defect is fixed, with the assertion message naming ESC-4 — so the fix cannot land silently and cannot be forgotten. Filed to the owning lanes as REQ-22. No invariant is affected: 18/18 PASS, BLOCKING=0.
- [2026-09-03T07:15:00Z] BLOCKER (observed live, not worked around) | L6 | **The tree is being written by another lane WHILE the L6 checkpoint verification runs, and the shared secret-scan gate is red because of it.** Between 06:20 and 06:58 UTC, five new files appeared in `tests/security/` - `test_sweep6_gate_bypass.py`, `test_sweep6_state_binding.py`, `test_sweep6_api_surface.py`, `test_sweep7_adversarial_input.py`, `test_sweep7_redaction.py` - and two of them were rewritten again inside the last three minutes of L6's run. That is L5's sweeps 6 and 7, which progress.md schedules for exactly this wave, so it is correct work in the correct path and NOT a lane-boundary violation. It is a **checkpoint-sequencing** problem, and it has two concrete effects on the numbers this checkpoint is supposed to certify. (1) **`python scripts/secret_scan.py --all` is RED: 6 findings in 1 location**, all of them fixture literals at `tests/security/test_sweep7_redaction.py:48,131-135` (an `sk-` key, a `ghp_` token, an `xox` token, a JWT, a PEM header and a `postgresql://admin:...@` URL) that do not yet carry the inline `# secret-scan: allow` marker. `--paths tests/integration ledger` is clean (17 files), so nothing L6 wrote contributes. **This is the third occurrence of the identical pattern** - REQ-5 against `tests/audit/test_redaction.py` and REQ-16 against `tests/contracts/test_schemas.py` were the first two - and the remedy is the same one-line marker each lane applies to its own file. (2) **The full-suite total moved under the run.** L6's verification run reports 2030 passed / 1 skipped / 44 xfailed / **0 failed**, against a stated baseline of 1929/1/41/0 plus L6's own 64; the extra ~37 passed and 3 xfailed are L5 files that were created after collection began, so the number is a snapshot of a tree that was changing, not a checkpoint measurement. Nothing failed in it, and the invariants (18/18 PASS, BLOCKING=0) and `tests/integration` (64 passed) were re-run afterwards and are stable. | **A human should decide the sequencing rule, because the technical fix is trivial and the process one is not.** ESC-3 established "one orchestrator session per working tree" and that is holding - there is no second orchestrator here. What is not established is that **a checkpoint measurement needs a quiet tree**: L5 and L6 were dispatched into the same wave by design (progress.md, Sprint 3 dispatch: "L5 is first in the S3 checkpoint wave" and "L6 runs at the S3 checkpoint alongside L5"), and L6's job is to certify totals for a tree that L5 is still adding tests to. Either L5 lands and is verified before L6 measures, or L6's totals are explicitly a moving target and the checkpoint number comes from a clean clone after both. The second is what ESC-3 already chose for commits; it should be said out loud for lane dispatch too. Separately, three lanes in three sprints have now shipped a credential-shaped fixture without the documented marker, which is a signal about the guidance, not about three lanes' carelessness: `CONTRIBUTING.md` should say, where fixtures are discussed, that a credential-shaped literal needs `# secret-scan: allow` on its line. | L6 changed nothing outside `tests/integration/` and `ledger/`. It did NOT add the marker to L5's file (not its path, and REQ-5 recorded why doing that for another lane is the wrong move), did not exclude the path from the scanner, and did not report the gate as clean. Filed to L5 as REQ-26. L6's own paths: `secret_scan.py --paths tests/integration ledger` clean, `ruff check tests/integration` clean, 64 tests green, invariants 18/18 PASS BLOCKING=0, `generate_schemas.py --check` 9 up to date and `git status --porcelain contracts/ tests/invariants/` empty, so the freeze is intact as of this entry.
- [2026-09-03T07:20:00Z] VIOLATION (live, uncommitted, found by the L6 freeze audit) | L6 reporting; author unknown | **A FROZEN file is being edited right now, with no escalation: `tests/invariants/_support.py` has 63 lines appended in the working tree.** `git status --porcelain tests/invariants/` reports ` M tests/invariants/_support.py` and `git diff HEAD` shows a single hunk, `@@ -448,3 +448,63 @@`, adding four helper functions after the existing content: `path_and_aggregate_policy`, `empty_book`, `full_book` and `unstressed_context`. File mtime 2026-09-03T00:59:17Z - roughly one minute before L6 found it, and in the same minutes that five new `tests/security/` files were appearing, so a lane is mid-write as this is written. The author is not identifiable from the tree; L6 did not write it and does not own the path. **Assessment, which is the part that matters: it is additive and behaviour-preserving.** The hunk touches no existing line - the three context lines above it are unchanged - and it redefines nothing: a duplicate-name check over every top-level `def`, `class` and constant in the file comes back empty, so no existing helper is shadowed and no invariant's behaviour can have moved. The four new functions are currently dead code: `grep` across the repository finds no importer outside the file itself, `tests/invariants/` still contains exactly 18 test modules, and the suite is 18/18 PASS BLOCKING=0 with the file in this state. So nothing is weakened today. | **Two things need a human, and the second is bigger than the first.** (1) The freeze rule was breached again, silently, and this is the fifth frozen-path write of the build - ESC-1 (escalated), the B905 edit in test_10 (recorded after the fact), `contracts/CANONICAL.md` and `contracts/README.md` (never escalated; see the L6 freeze audit above), and now this. A rule breached five times and escalated once is not being enforced by anything, and there is a mechanical fix: a pre-commit and CI check that fails on any diff under `contracts/` or `tests/invariants/` without a matching escalation entry. The freeze currently depends on lanes remembering it, and it has now been shown five times that they do not. (2) **The docstrings say what this is for: "so invariants 19-22 vary ONE thing" and "the baseline for 19-22".** Somebody is about to add four NEW invariant test files to the frozen directory. That is a far larger event than a helper append, and it is the one that should be approved before it lands, not after: new invariants change what BLOCKING means, change the INVARIANT TOTALS line every lane gates on, and - if any of the four does not pass - halt every lane's merges. Whoever is writing 19-22 should say so in this ledger first, with what each one asserts and why it is not already covered by 01-18. L6 has no view on whether they are good ideas; it has a strong view that they should not appear in a frozen directory unannounced. | L6 **left the file exactly as found**. It did not revert it (reverting is a second unescalated write to the same frozen file, which is the reasoning already recorded for the B905 edit), did not commit it, and did not stage anything. The L6 freeze audit recorded immediately above this entry covers `9eebf24..HEAD` and is therefore unaffected: this change is in the WORKING TREE only and is in no commit. Re-verified after finding it: invariants 18/18 PASS, PENDING-IMPL=0, BLOCKING=0; `git status --porcelain contracts/` empty; `generate_schemas.py --check` 9 up to date. **Note for the next auditor: `git diff <commit> HEAD` on the frozen paths is not sufficient. Run `git status --porcelain` and `git diff HEAD` on them as well, or a live uncommitted edit like this one is invisible to the audit.**
- [2026-09-03T02:40:00Z] AUTHORIZED FROZEN-PATH CHANGE | ORCH (on explicit human instruction) | The human directed "Add the missing invariants as xfail BEFORE the features land" and "Resolve ESC-1 with the permanent guard". Both require writing inside `tests/invariants/`, which is FROZEN and read-only to every lane. Recording the authorization because the freeze is what makes an invariant trustworthy, and a human override of it must be visible in the same ledger a reader checks for tampering. | **Count correction, reported rather than guessed:** the instruction said 6 missing (18 -> 24). Dispatch Addendum section 2 lists 6 new invariants, but one of them - `semantic_layer_disabled_produces_identical_verdict` - already shipped as INV-18 at the freeze. So 5 were missing, and the target is **23**, which is what the Addendum itself states ("Total invariant count is now 23"). A 24th was not invented to make the arithmetic work; if the human has a 24th in mind it is not in the Addendum this run holds. | Added INV-19 aggregate_check_can_override_per_agent_pass, INV-20 size_scales_down_as_drawdown_deepens, INV-21 authorization_bound_to_state_hash, INV-22 authorization_invalid_after_state_change, INV-23 no_llm_in_deterministic_path, plus four shared builders in `_support.py`. They were written to the Addendum's wording FIRST and run against the code afterwards: all five PASS unmodified, so they document behaviour L1/L2/L3 already implement correctly and now lock it in. **None was weakened to go green, and none needed an xfail.** Totals: 23/23 PASS, BLOCKING=0, 82 tests.
- [2026-09-03T02:42:00Z] ESC-1 CLOSED | ORCH | ESC-1 (a hypothesis strategy generating `advised=0`, handed to a helper that builds a REDUCE opinion, which the contract refuses because a REDUCE to zero is semantically a REJECT) was previously resolved by narrowing that one strategy's bound to `min_value=1`. That fixed the instance, not the class. | The failure mode was never the bound: it was that an invalid combination could be CONSTRUCTED at all, so the contract's refusal surfaced as a bare ValidationError deep inside an unrelated assertion, where it read like a product defect. Any future widened bound, new strategy or new test reopens it. | Permanent guard added at the single construction point every invariant uses, `tests/invariants/_support.opinion()`: a REDUCE with a non-positive or absent quantity now raises immediately with a message naming the rule, pointing at ESC-1, and telling the caller to use `opinion('REJECT')` instead. Two regression tests in test_01 pin it - one that the guard fires for None/"0"/"-1", one that the CONTRACT (not the guard) remains the actual authority. The guard is a readability device; enforcement stays in the contract. ESC-1 is closed.
- [2026-09-03T07:30:00Z] CORRECTION + RE-AUDIT | L6 | **The [2026-09-03T07:20:00Z] VIOLATION entry above is SUPERSEDED: the frozen-path write was authorized, and the authorization entry landed after L6 observed the file.** While L6 was verifying, commit `f30f119` ("invariants: 18 -> 23 (Addendum section 2) and close ESC-1 with a permanent guard") landed, moving HEAD from `6b1a800`. It carries the ORCH entry at [2026-09-03T02:40:00Z] AUTHORIZED FROZEN-PATH CHANGE, recording explicit human instruction to add the missing invariants and to close ESC-1 with a permanent guard. L6 saw `_support.py` mid-write, before that entry existed in the file, and reported it as unescalated. That was accurate at the instant of observation and is wrong now, so it is corrected here rather than left to be discovered - and it is worth noting WHY it read as a violation for several minutes: an authorization written in the same commit as the change it authorizes is invisible to anyone watching the working tree, which is exactly where a freeze is supposed to be observable. The authorization entry should land BEFORE the first write, in its own commit; that is the only ordering under which the freeze is checkable by a third party rather than only explicable afterwards. **The substantive point in the superseded entry stands in full: the build has now produced six frozen-path writes and only three carried an entry at the time. The mechanical remedy is unchanged - a pre-commit and CI check that fails on any diff under `contracts/` or `tests/invariants/` without a matching escalation entry.** | Nothing further for a human on the authorization itself. | **RE-AUDIT of `6b1a800..f30f119` on the frozen paths, complete, every hunk accounted for: nine files, 552 insertions, ONE deletion, and every change is ADDITIVE or STRENGTHENING. None weakens anything.** (1) `tests/invariants/test_01` — two new tests appended, no existing line touched: one pins the new ESC-1 guard, one asserts the CONTRACT itself still refuses `AdvisoryOpinion(recommendation="REDUCE", recommended_quantity="0")`, constructing the object directly so it bypasses the guard and tests the authority rather than the convenience. That is the same property L6 verified independently by probe during its own audit, and it is now permanently pinned; strictly stronger than before. (2) `tests/invariants/_support.py::opinion` — a new precondition raising `AssertionError` for a REDUCE to a non-positive quantity. It can mask nothing: the contract's refusal is asserted separately by (1), and a guard that fires only on inputs the contract already refuses removes no coverage. The single "deletion" in the diffstat is the docstring line it replaced. (3) `tests/invariants/_support.py` — four new fixture helpers, appended, redefining nothing (duplicate-name check over every top-level definition is empty). (4) `tests/invariants/conftest.py` — five rows added to the INVARIANTS registry; the PASS / PENDING-IMPL / BLOCKING classification logic is untouched, so the meaning of the TOTALS line is unchanged and only its denominator moved. (5) Five new invariant modules, 19-23. L6 read 20 and 23 closely and skimmed 19, 21 and 22: they are non-vacuous and each carries its own anti-vacuity guard, which is the thing most likely to be missing from a hastily added invariant. INV-20 asserts monotonic non-increase across a nine-step drawdown ladder AND a strict decrease end to end, explicitly so that "an engine ignoring path state would satisfy monotonicity vacuously" cannot pass, plus two contract-level refusals of a ladder that scales up or is unsorted. INV-23 is a static AST scan for LLM and network imports in `mizan/risk`, `mizan/policy` and `mizan/governor`, and carries `test_the_check_actually_inspects_something`, which asserts the scan found modules and parsed real imports — the failure mode of every static check. Both are the right shape. **INVARIANT TOTALS after the re-audit: PASS=23 PENDING-IMPL=0 BLOCKING=0 NOT-RUN=0 (of 23), 82 tests.** Separately, and recorded because ESC-3 is about exactly this: `f30f119` also committed `ledger/escalations.md` while L6 had four unpushed entries in it. Nothing was lost — all four are in HEAD and verified present — but they were committed by a session that did not write them and did not know they were there, which is the `git add -A` hazard ESC-3 resolved to stop. `ledger/learnings.md` and `ledger/requests.md` still hold L6's uncommitted entries; `tests/integration/` is still untracked. L6 has committed nothing.
- [2026-09-03T07:40:00Z] INFO (handover note) | L6 | **The tree is still growing at L6 handover; the numbers in the L6 progress entry are timestamped to HEAD `f30f119` and to nothing later.** In the minutes after L6 finished measuring, invariant **24** (`advisory_reduce_to_zero_is_rejected_as_invalid`, tagged `ESC-1/E1`) appeared with a matching row in `tests/invariants/conftest.py`, and `contracts/determinism-reference.json` plus `scripts/determinism_fingerprint.py` appeared and then moved. All of it is inside the frozen paths and all of it is presumably covered by the [2026-09-03T02:40:00Z] AUTHORIZED FROZEN-PATH CHANGE entry. L6 re-ran the gate after each observation and it stayed green: **INVARIANT TOTALS PASS=24 PENDING-IMPL=0 BLOCKING=0 NOT-RUN=0 (of 24), 101 tests**, and `tests/integration` 64 passed, unaffected because it imports nothing from `tests/invariants`, `tests/fixtures` or `tests/security`. L6 stopped chasing at this point: a critic cannot certify a tree that is being rewritten while it measures, and every finding it has is durable in this ledger regardless of what lands next. | **The one thing a human should take from this, and it is the same point as the [07:15:00Z] BLOCKER: the S3 checkpoint has no quiet moment in it.** L5, the invariant expansion and L6 all wrote and measured in the same window, and the totals moved three times under L6's runs. The fix is sequencing, not tooling: land and verify the additions, THEN measure. Until then, treat any checkpoint total in this ledger as "true at the named commit", never as "true now" - which is exactly what ESC-3 concluded about commits and should now be said about lane dispatch too. | Nothing reverted, nothing chased. Final L6 state at `f30f119`: full suite 2263 passed / 1 skipped / 56 xfailed / **0 failed**; `secret_scan.py --all` clean over 290 files; `ruff check tests/integration` clean; `generate_schemas.py --check` 9 up to date; `git status --porcelain contracts/` empty at the time of measurement. L6 committed nothing and staged nothing; `tests/integration/` (13 files, 64 tests) is untracked and `ledger/` carries L6's uncommitted entries.
- [2026-09-03T04:10:00Z] ESC-5 / AUDIT COMPLETE | ORCH | **ESC-4 class audit: every enabled blocking check driven against a hostile battery plus targeted exemplars, to find any other control that cannot fail.** Result: of 36 implemented checks, **35 are provably failable** and exactly **one is dead - `duplicate_order`**, the original ESC-4. Confirmed at the root rather than by symptom: `RiskContext.recent_orders` is a parameter of `BrokerContextProvider.build` (mizan/adapters/context.py:91) that defaults to `()` and is populated by NO caller anywhere in `mizan/`, so the loop body of `duplicate_order` - and the recent-orders arm of `erroneous_order` (checks.py:458) - is unreachable in production. The check logic itself is correct: given recent orders it fails correctly. The defect is the missing wiring. | Two systemic findings the audit turned up that were NOT in ESC-4: (1) `mizan/risk/__init__.py:150-162` - when a check function returns `None`, `_run` FABRICATES `passed=True` at the configured severity with every evidence field `None` and an empty detail. That is the ESC-4 signature generalised: a blocking PASS asserting a control ran when it did not. 17 of 36 checks have a `return None` path. (2) `restricted_symbol` and `restricted_strategy` took that path on every non-restricted proposal - 148 no-evidence blocking passes across the audit battery. | FIXED (2): both now return `ok(...)` carrying the count checked against and a stated detail; no-evidence passes are now **zero** across the whole battery. NOT fixed: `duplicate_order`, which needs the context provider to populate `recent_orders` from the broker - that is L3's path and a wiring change, not a check change, so it is left open and pinned. INV-25 added (see below); it excludes exactly one check, by name, and asserts that exclusion set is EXACTLY `{"duplicate_order"}` and that ESC-4 is named in this ledger - so a NEW dead check fails the invariant hard (the HALT), and fixing this one also fails it until the entry is removed. Deliberately NOT an xfail: this suite's conftest classifies a skipped or xfailed invariant as BLOCKING, correctly, because an invariant that does not run proves nothing.
- [2026-09-03T05:30:00Z] ESC-4 CLOSED | ORCH (on human instruction: "WIRE recent_orders, do not disable") | `duplicate_order` - the one check the ESC-5 audit proved could not fail - now can. `BrokerContextProvider` derives `RiskContext.recent_orders` from the tenant's own decision chain: newest-first, bounded by the policy's own `duplicate_order.window_seconds` (so a tenant widening the window widens what the check sees and the two cannot silently disagree), capped at 200 records, counting only executions that actually reached the venue (`SUBMITTED`, `RECONCILED_EXISTING`). `WOULD_SUBMIT` is excluded: a dry run placed nothing, and treating it as a live order would block real first orders. An absent or unreadable ledger yields an empty list rather than taking the decision path down - the check then reports what it always did, but visibly rather than structurally. | The check's LOGIC was correct all along; only the wiring was missing, which is why the instruction was to wire it rather than disable it. Disabling would have been the worse outcome: a disabled check is absent from the record and everyone knows it, whereas this one was writing `passed=True, severity=blocking` into records whose only purpose is to be believed later. | Proven end to end, not asserted: a proposal is recorded with a SUBMITTED execution, an identical proposal is then evaluated, `recent_orders` derives 1 order from the chain, `duplicate_order` returns `passed=False` with `DUPLICATE_ORDER`, and the evaluation verdict is REJECT. 8 regression tests in `tests/adapters/test_recent_orders_are_derived_from_the_ledger.py` cover the duplicate, the dry run, the out-of-window order, another tenant's order (B3 - invisible), an unreadable ledger, and no ledger at all. **The INV-25 ratchet fired as designed**: emptying `KNOWN_DEAD` turned the invariant BLOCKING until `duplicate_order` was observed failing on a real exemplar, and it is now green with `KNOWN_DEAD == {}`. A new dead check HALTS; the wiring itself is pinned by `test_the_wiring_that_closed_esc4_is_still_in_place`, so deleting the derivation reopens ESC-4 loudly rather than silently.
