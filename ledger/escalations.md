# Escalations ledger (append-only) — THE HUMAN READS THIS FIRST

Format: `[UTC timestamp] severity | lane | what | why it needs a human | what the Orchestrator did meanwhile`

- [2026-09-02T14:32:10Z] INFO | ORCH | Docker Desktop daemon is not running on this machine | Postgres per-tenant schema isolation (B3) is authored as SQL + docker-compose but cannot be executed or tested in this run; SQLite one-file-per-tenant ledger is the tested isolation for now | Continued; L0 ships the compose base and init SQL untested and says so in progress.md.
- [2026-09-02T14:32:10Z] INFO | ORCH | TradingAgents licence is unverified (Master Plan §14) | A human must confirm the licence before the W8 adapter ships in a release | L3 builds the adapter against a local stub; it is not marked shippable.
- [2026-09-02T14:41:09Z] INFO | ORCH | The "Killer Feature Verdict" document arrived truncated (cut inside §7 "The demo, revised"; the 50k-char message limit). §1–§6 were received and applied | The revised demo script and any later sections are unknown to this run | Saved the received portion as docs/MIZAN-KILLER-FEATURE-VERDICT.md; killer demo continues to follow Master Plan §11. Please drop the full file into docs/.
- [2026-09-02T14:41:09Z] DECISION | ORCH | Risk Canon P0 items (path-dependence state, aggregate multi-agent layer) are incorporated BEFORE the contract freeze by making them RiskContext inputs (engine stays a pure function). Recorded as ADR-0006 and docs/API-SURFACE-ADDENDUM-1.md | A human may prefer a stateful engine object instead; this run chose the pure-function form because it is the only one that keeps A1 (byte-identical replay) provable | Continued.
- [2026-09-02T23:25:21Z] ESC-1 RESOLVED | ORCH | A FROZEN file (tests/invariants/test_01) was edited. Recording it because the freeze exists so this never happens silently. | The hypothesis strategy generated `advised=0` and passed it to a helper that builds an AdvisoryOpinion with recommendation=REDUCE. The contract refuses that combination — "advisory REDUCE requires a positive recommended_quantity" — because a REDUCE to zero is semantically a REJECT. So the generator produced an input the contract forbids: a defect in the test's own strategy, not in any implementation. The alternative readings were both worse: relaxing the contract to allow REDUCE-to-zero would create two ways to express one outcome and weaken E1's clarity, and leaving the suite BLOCKING would halt every lane's merges over a generator bound. | Changed `min_value=0` to `min_value=1` and documented why in the test docstring. The assertion is untouched and exactly as strong: no advised quantity, however large, lifts the authorized quantity above the deterministic cap. The advised-zero case remains covered by the REJECT path in invariant 02 and by the ValidationError cases in the same file. Reviewed and approved by the Orchestrator per §3 (a contract/invariant change requires escalation, not silence).
- [2026-09-02T23:31:56Z] BLOCKER | L0 (infra) | The CI `secret-scan` job is red: `tests/audit/test_redaction.py:24` commits a module-level `sk-` prefixed fixture literal, which `scripts/secret_scan.py --all` correctly reports. Nothing merges until it carries an inline `# secret-scan: allow` marker. | The file belongs to L2b and L0 will not edit another lane's paths, will not create a repo-root allow-list that exempts it, and will not relax the scanner's placeholder rules to accommodate it - all three hide the finding rather than resolve it. A human or the Orchestrator should route the one-line fix. | Raised as REQ-5 and reported in L0's handover. L0's tests/infra suite asserts the same condition CI asserts, so the red is visible rather than latent; scanner self-test and --history are clean.
- [2026-09-02T23:31:56Z] BLOCKER | L0 (infra) | The CI `lint` job cannot pass and no lane may fix it: ~250 of 313 ruff findings are in FROZEN or read-only paths (mizan/contracts, tests/invariants, tests/fixtures, tests/security). 224 are E501 and the longest line is 186 characters, so no line-length setting clears them. | Every available remedy is above this lane's authority: sweeping the frozen paths edits frozen files, excluding them from ruff permanently narrows the lint gate, and marking the job non-blocking weakens a CI gate. The Orchestrator must choose. | Raised as REQ-6. L0 fixed its own 9 findings in scripts/ (all E501, wrapping only, no behaviour change) and ships tests/infra clean; nothing else was touched.
- [2026-09-02T23:32:43Z] DECISION | L2a (governor/advisory/authorization) | docs/API-SURFACE.md §3.3 says the governor should add ADVISORY_UNAVAILABLE to GovernorDecision.reason_codes as an informational code when the advisory is absent/unavailable and policy.advisory.enabled is true. It does NOT do that, and cannot. | Invariant 18(c) (FROZEN) records a decision made with an advisory CONCUR, then asserts that replay(record, advisory=None) reproduces the recorded verdict_hash. verdict_hash covers reason_codes, so an informational code emitted for advisory=None but not for CONCUR makes those two hashes differ and INV-18 fails. Invariant 13 independently requires govern(advisory=None) and govern(unavailable opinion) to produce the same verdict_hash, so the code cannot be emitted for one and not the other either. The three positions are mutually exclusive; the invariants win. A human should ratify the doc change. | Implemented the invariants: ADVISORY_UNAVAILABLE appears ONLY on the fail-closed REJECT path (policy.advisory.enabled AND fail_closed.on_advisory_unavailable), which INV-13 requires. The 'the advisory was not consulted' fact is still in the record: GovernorDecision.llm_advisory carries invoked/available/profile and the reason the opinion was unusable, which is what the console renders. Suggested doc amendment: strike the parenthetical from §3.3 and say instead that unavailability is recorded on llm_advisory, never in reason_codes, except when it changes the verdict.
- [2026-09-03T00:22:00Z] DECISION | ORCH (resolving REQ-6 from L0) | The `lint` CI job could not pass and no lane was permitted to fix it: ~250 of 295 ruff findings sat in FROZEN or read-only paths (mizan/contracts, tests/invariants, tests/fixtures, tests/security), 215 of them E501, with a 186-character longest line that no line-length setting clears. L0 correctly refused all three obvious remedies and escalated. | A gate that cannot be satisfied is not a gate — it trains everyone to ignore a red job, which is worse than not having it. But the two easy fixes each destroy something: sweeping the frozen paths means a large mechanical reformat of reviewed, frozen files, which is precisely the diff a real change hides in (the reason the freeze exists), and `extend-exclude` means those paths are never linted again, including for genuine defects. | RESOLVED with the narrow option, now in pyproject.toml: `per-file-ignores` on the four frozen paths listing ONLY the cosmetic codes — E501 (line length), I001 (import order) and the UP modernisation rules. `F` (undefined and unused names) and `B` (likely bugs) stay fully enforced in those paths, and every non-frozen path keeps the complete rule set. `ruff check .` now reports 27 findings, all auto-fixable, all inside the L3/L4 stub files being rewritten this sprint, and each of those lanes has "ruff clean on your own paths" in its DoD. To revisit when the freeze lifts at the next schema version. This is a consequence of the freeze, not a lowering of the standard.
- [2026-09-03T00:23:00Z] INFO | ORCH | REQ-5 (the secret-scan BLOCKER L0 raised) is closed and needed no Orchestrator routing: L2b added the inline `# secret-scan: allow` marker to its own fixture line, which was the remedy L0 recommended and deliberately declined to apply itself. `secret_scan.py --all` is clean across 213 files, `--history` and `--self-test` clean. | No human action needed; recorded because the BLOCKER was recorded. | Continued.
- [2026-09-03T00:24:00Z] DECISION | ORCH | Granted one agent joint write access across a lane boundary (mizan/contracts/canonical.py, which is L0's, plus mizan/audit and tests/audit, which are L2b's) to close REQ-2 and REQ-3. | The 24 gap tests in tests/audit/test_redaction.py are `xfail(strict=True)`, so an xfail that starts passing FAILS the suite. Closing a redaction gap in L0's `redact()` therefore turns L2b's suite red in the same instant, and neither lane can land a green change alone. Splitting the work across two agents guarantees a red checkpoint; the §5.2 ownership rule exists to prevent collisions, and here it would cause one. The Orchestrator is the router for cross-lane requests (§5.2), so this is a routing decision, not an exception to the rule. | Authorization is scoped to REQ-2 and REQ-3 only and stated as such in the brief. `contracts/*.schema.json`, `contracts/reason_codes.json`, `contracts/error_codes.json` and `tests/invariants/` remain frozen and off-limits; "regenerating schemas produces zero diff, proven by an empty `git status --porcelain contracts/`" is an explicit DoD item.
- [2026-09-02T23:51:59Z] ESC-2 RESOLVED | L2a → ORCH | API-SURFACE §3.3 required adding ADVISORY_UNAVAILABLE as an informational reason code whenever the advisory was enabled but absent. L2a reported it unimplementable and was right. | verdict_hash covers reason_codes, so an absent advisory would hash differently from a CONCUR advisory that produced the same outcome — breaking invariant 18(c) (a recorded CONCUR must replay identically with advisory=None) and invariant 13. The invariants describe the product guarantee; the doc line described a nicety. | Struck the clause from docs/API-SURFACE.md §3.3 and recorded the general rule it taught: REASON CODES DESCRIBE THE OUTCOME, NEVER THE CIRCUMSTANCES THAT PRODUCED IT. Circumstances belong on GovernorDecision.llm_advisory, which is recorded but not hashed into the verdict. Unavailability is still fully auditable there.

- [2026-09-03T00:32:00Z] BLOCKER (worked around) | L3a (execution gate + adapters) | **Two agents were writing lane L3a's paths at the same time.** Throughout this run, `mizan/execution/`, `mizan/adapters/`, `tests/execution/` and `tests/adapters/` were being created and rewritten by another process concurrently with mine — file mtimes advanced between consecutive tool calls, and three of my edits returned "the file changed on disk since you last read it". `ledger/progress.md` records exactly one L3a dispatch, so this is a duplicate/parallel run of the same brief rather than a lane-boundary violation. | Real damage occurred before I detected it: I overwrote a `tests/execution/conftest.py` the other run had just written, its test modules were then re-pointed at MY fixture names, I restored ITS fixture names, and both test files went red — a two-cycle thrash on one shared file that cost several minutes and could have been committed red. The general hazard is worse than the specific one: two writers on one file produce a plausible-looking interleaving that no test catches, which is precisely the failure mode the §5.2 one-lane-one-owner rule exists to prevent. | Recovered by converging rather than fighting: both runs independently moved every test module to a **self-contained** form (each builds its own chain, gate and doubles; no shared fixtures), after which the shared `tests/execution/conftest.py` was dead code and I deleted it. From that point the two runs could only collide at whole-file granularity, which is recoverable. Final state is green and stable across repeated runs — invariants 18/18 PASS BLOCKING=0, tests/execution + tests/adapters 169 passed, ruff clean, secret scan clean. **Ask of the Orchestrator:** confirm only one L3a result is merged, and diff the two runs' outputs before the checkpoint if a second report exists; and gate future waves so one lane is dispatched once.
- [2026-09-03T05:45:00Z] BLOCKER-AVOIDED | L3b (sdk/api) | **Two agents were running as lane L3b in the same working tree at the same time.** This session found `mizan/sdk/__init__.py`, `mizan/api/__init__.py`, `mizan/api/auth.py` and `mizan/api/ratelimit.py` already written and being rewritten — file mtimes moving seconds apart while this agent was reading them — and later `tests/sdk/test_pipeline.py` and `tests/api/test_auth_and_isolation.py` appeared from the other agent mid-run. Nothing was lost, but only because this session changed method on discovering it. | A human should decide whether the duplicate dispatch was intended (a retry after a presumed-dead agent) and, if not, how lane dispatch is de-duplicated. The failure mode is not hypothetical: two agents doing whole-file `Write` on one module destroy each other's work silently, and the loser's tests keep passing against the winner's code, so the loss is invisible at the checkpoint. | This session adopted a **no-clobber protocol** for the rest of its run and recommends it as the standing rule for any shared tree: create NEW files with `Write`; change EXISTING files only with exact-match `Edit` or an explicit in-place patch, which fails loudly on a concurrent rewrite instead of overwriting it; re-check mtimes before each edit to a contested file. Every change this session made to the other agent's files is additive or a bug fix, and both agents' test files now coexist and pass together (260 tests across tests/sdk + tests/api). The other agent's implementation was kept as the base and NOT overwritten.
- [2026-09-03T05:45:00Z] DECISION | L3b (sdk/api) | `@mizan.protected` now hands the wrapped function the **authorized** proposal rather than the one the agent proposed, and refuses outright when `ExecutionConfig(dry_run=False)`. | Recording it because both were behaviour changes to code another agent had already written, and both were defects rather than preferences. (1) The decorator previously called `fn(proposal, ...)` with the ORIGINAL proposal after the gate approved. On a REDUCE verdict — governor authorizes 4 of the 10 shares asked for — the developer's own `broker.submit(order)` would have submitted 10. A reduction that is only reported is not a reduction; the object handed to the caller now carries the authorized leg quantities. (2) With `dry_run=False` the gate submits through Mizan's own broker AND the decorator then called the caller's submit function, placing a second order for the same authorization. That combination is now a `ConfigurationError` instead of a documented caveat. | Both are pinned: tests/sdk/test_protected_decorator.py::test_the_function_receives_the_authorized_quantities_not_the_proposed_ones and ::test_protected_refuses_a_configuration_that_would_double_submit. Invariants unaffected (18/18 PASS).
- [2026-09-03T05:45:00Z] INFO | L3b (sdk/api) | `mizan.api` had a latent defect that made every route taking the request object unusable: FastAPI was imported inside `create_app`, but the module uses `from __future__ import annotations`, so FastAPI resolved `request: Request` against the module globals, found nothing, and reinterpreted it as a required **query parameter** — `POST /v1/proposals/evaluate`, `POST /v1/control/kill-switch` and `GET /v1/health` all answered 422 before their first line ran. | Recorded because of how it hides: the package imports, the app builds, every route registers, and nothing complains until a real request arrives. Any lane using `from __future__ import annotations` in a module with framework-resolved annotations has the same trap. | Fixed by moving the import to module scope behind a guard (`FASTAPI_AVAILABLE`), so `create_app` still degrades with a clear `ConfigurationError` when the transport extra is absent while the framework-free parts — token store, rate limiter, hardening middlewares, `ROUTES` — keep working. Pinned at the signature level by tests/api/test_route_signatures.py, which asserts no route's request or principal parameter was demoted to a query param.
- [2026-09-03T01:05:00Z] VIOLATION (recorded, not reverted) | ORCH | A FROZEN file was edited with no escalation: `tests/invariants/test_10_hash_chain_verifies.py` gained `strict=False` on a `zip(...)` plus a two-line comment. It arrived inside the Orchestrator's own S2 checkpoint commit 48969ed, having come up from an S2 lane's working tree, so the Orchestrator committed a frozen-path edit without noticing. | The change is semantically null — `strict=False` is `zip`'s default, so the pairwise walk and both assertions are byte-for-byte equivalent in behaviour — and no invariant was weakened. It is recorded anyway, because the freeze exists precisely so that "harmless" frozen-path edits cannot accumulate unremarked, and because the ROOT CAUSE is an Orchestrator decision, not a lane's carelessness: the REQ-6 resolution kept ruff's `B` (likely-bug) rules enforced on the frozen paths, `B905 zip-without-explicit-strict` therefore fired inside tests/invariants, and a lane did the obvious thing and fixed the lint it was shown. A gate that reports a fixable finding in a file nobody may edit will keep producing exactly this. | Left in place: reverting would be a second unescalated edit to the same frozen file for no behavioural gain. REQ-6 amended — `B905` is added to the frozen-path per-file-ignores so the gate stops soliciting edits it forbids; `B` otherwise stays enforced. Lesson for ledger/learnings.md: when exempting frozen paths from a gate, exempt every rule the gate can actually raise there, not only the cosmetic ones.
- [2026-09-03T01:10:00Z] HALT-CLASS | ORCH | **A SECOND ORCHESTRATOR SESSION IS BUILDING THIS REPO CONCURRENTLY, IN THE SAME WORKING TREE.** Peer session `mizan-04` (interactive, started ~9h ago) is the session that produced the entire commit history under the identity `Mizan Orchestrator <orchestrator@mizan.local>` (c549b2a … f34cbd7). It is still active: it dispatched its own Sprint 3 lanes onto the same paths as this session's, and it committed `8cdb610` mid-sprint. Two of this session's agents independently reported a "duplicate agent" rewriting their files — those were its agents, not a dispatch fault here. | This is the §5.2 one-writer-per-path rule broken at SESSION level, where no lane discipline can reach it. Concrete damage already observed and recovered: a `tests/execution/conftest.py` written by one run was overwritten by the other, its tests re-pointed at the wrong fixtures, and both files went red before the two runs independently converged on self-contained test modules. `8cdb610` is worse in kind — it was made with `git add -A` while four lanes were mid-write, so it committed a partial cross-lane snapshot that passed through no checkpoint gate. The next such commit may not land green, and two writers on one file can produce a plausible interleaving that no test catches. | This session STOPPED dispatching new work. Its two in-flight agents (L3b SDK/API, L4 console) were left to finish rather than killed, because killing a mid-write agent leaves half-written files, which is the worse failure. No history was rewritten and `8cdb610` was left in place (Hard Rule: never rewrite history). Escalated to the human: only one orchestrator session may hold this working tree, and a human must choose which. Full state at the moment of detection: invariants 18/18 PASS BLOCKING=0, full suite 1926 passed / 2 failed, frozen schemas verified identical (`generate_schemas.py --check`: 9 up to date).
