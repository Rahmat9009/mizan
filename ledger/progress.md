# Progress ledger (append-only)

Format: `[UTC timestamp] sprint | lane | task | DoD met (y/n) | notes`

- [2026-09-02T14:32:10Z] S1 | ORCH | Boot: read Master Plan v2 (§4,§5,§8), contracts (none yet), README/spec/decision, app/ (Rahmat's legacy governor) | y | Repo initialised on `main` tracking Rahmat9009/Mizan- `master`. Plan moved to docs/.
- [2026-09-02T14:32:10Z] S1 | ORCH | Environment: git 2.47, Python 3.12.4, pytest 9, pydantic 2.13.4, jsonschema 4.26, hypothesis 6.157, docker CLI present but daemon NOT running, no gh CLI | y | Postgres compose base will be authored but cannot be exercised this run.
- [2026-09-02T14:43:30Z] S1 | L0-A | Dispatched: contracts/*.schema.json (8 + control_event), reason/error taxonomies, CANONICAL.md, mizan/contracts pydantic types, lane stubs, tests/contracts, tests/fixtures | pending | Addendum 1 sent mid-task.
- [2026-09-02T14:43:30Z] S1 | L0-B | Dispatched: tests/invariants (17 + #18 semantic_layer_disabled_produces_identical_verdict), conftest INVARIANT STATUS summary, README | pending | Addendum 1 sent mid-task.
- [2026-09-02T14:43:30Z] S1 | L0-C | Dispatched: pyproject, Makefile, CI workflow (incl. postgres-ddl job), secret scanner + git hook, docker-compose + per-tenant Postgres init SQL, ADRs 0000–0006, CONTRIBUTING, tests/infra | pending | Addendum 1 sent mid-task.
- [2026-09-02T14:43:30Z] S1 | L5 | Dispatched: Sweep 1 (secrets incl. git history) + Sweep 2 (sensitive data flow, legacy redaction tests) + THREAT-MODEL.md skeleton | pending | Flag-only authority.

## Sprint 1 checkpoint — [2026-09-02T22:57:44Z]
- [2026-09-02T22:57:44Z] S1 | ORCH | Four L0/L5 agents terminated early on a session rate limit. Orchestrator completed the gaps directly: 12 lane stubs, schema generator, 9 JSON Schemas, 2 real bug fixes | y | See learnings.
- [2026-09-02T22:57:44Z] S1 | L0 | contracts/ + mizan/contracts + tests/fixtures + tests/invariants (18) + CI/infra scaffolding | y | Commit 9eebf24.
- [2026-09-02T22:57:44Z] S1 | ORCH | **CONTRACTS FROZEN at 9eebf24.** contracts/*.schema.json, contracts/reason_codes.json, contracts/error_codes.json and tests/invariants/ are READ-ONLY from now on. Changes require a HALT entry in ledger/escalations.md and Orchestrator approval. | y | Announced per §10 step 9.
- [2026-09-02T22:57:44Z] S1 | GATE | Invariants: PASS=2 (15 no-binary-float, 16 no-live-trading), PENDING-IMPL=16, BLOCKING=0. Legacy 354 passed/1 skipped. Security 63 passed/41 xfail. Secret scan clean on tree and history. | y | Green checkpoint; Sprint 2 dispatched.
- [2026-09-02T23:05:43Z] S2 | ORCH(L0 gap) | contracts/CANONICAL.md (hash derivations + a standalone verifier proven to accept a good chain, catch tampering at record 3 and catch deletion at record 4), contracts/README.md, tests/contracts (42 tests) | y | Freeze guard now active: regenerating schemas must produce no diff.
- [2026-09-02T23:08:48Z] S2 | L5 | Sweeps 1-2 complete (24 findings: 4 CRITICAL, 8 HIGH, 10 MEDIUM/LOW + 2 informational), THREAT-MODEL.md skeleton (15 W11 threats), 104 security tests pinning legacy behaviour | y | All CRITICAL/HIGH are LEGACY findings with do-not-repeat instructions; none open against new core code. Git history confirmed free of secrets — nothing to rotate.
- [2026-09-02T23:08:48Z] S2 | ORCH | Triaged and routed all L5 findings to the owning lanes; F-1/F-2 relayed to L1 in flight, F-5/F-6/F-7 to L2b in flight | y | See security/findings.md F-25.
- [2026-09-03T05:10:00Z] S2 | L1 | mizan/policy (Decimal-preserving loader, fail-closed validate_policy, hash/diff/InMemoryPolicyStore), mizan/risk (36 of 43 checks, pure engine), policies/*.yaml, tests/policy (77) + tests/risk (165) | y | Invariants 03/04/05/13/17/18 and 01/02/08/09/10/15/16 PASS; the 5 still PENDING-IMPL are all on L3's NotImplementedError. Legacy 354 passed/1 skipped unchanged. Valuation comes only from context snapshots (F-1/F-2), pinned by tests/risk/test_valuation_is_not_caller_controlled.py.

## Sprint 2 checkpoint — [2026-09-03T00:20:00Z] — commit 48969ed
- [2026-09-03T00:20:00Z] S2 | GATE | Invariants PASS=13 PENDING-IMPL=5 BLOCKING=0. The 5 pending (06, 07, 11, 12, 14) are blocked on exactly one thing: `mizan/execution/ExecutionGate.execute` still raises NotImplementedError. Nothing else in the tree is red. | y | Green checkpoint; all four S2 lanes merged to main.
- [2026-09-03T00:20:00Z] S2 | L1 | mizan/policy (DSL loader, validator, versioning, hash-signing) + mizan/risk (12 deterministic checks, path-dependence sizing, aggregate layer, valuation sourced only from context snapshots) | y | 77 policy + 164 risk tests green. F-1/F-2 (caller-controlled valuation and market risk) provably not repeated in the new core: tests/risk/test_valuation_is_not_caller_controlled.py.
- [2026-09-03T00:20:00Z] S2 | L2a | mizan/governor (arbitration) + mizan/advisory (offline + OpenAI-compatible provider) + mizan/authorization (issue/validate/consume, scope and state binding) | y | 36 + 63 + 37 tests green. One DECISION escalated: ADVISORY_UNAVAILABLE cannot be an informational reason code without breaking INV-13 and INV-18; implemented the invariants, doc amendment proposed.
- [2026-09-03T00:20:00Z] S2 | L2b | mizan/audit (hash-chained append-only ledger, SQLite append-only triggers, redaction applied to the WHOLE record before hashing) + mizan/replay + verify-chain CLI | y | 113 tests green, 24 strict xfails pinned to REQ-2. F-7 closed by construction.
- [2026-09-03T00:20:00Z] S2 | L0 | tests/infra (256), docs/adr, CONTRIBUTING.md, ruff per-file-ignores for frozen paths | y | 42 contract tests green; regenerating schemas still produces no diff.
- [2026-09-03T00:20:00Z] S2 | ORCH | REQ-5 (secret-scan blocker) verified CLOSED — the inline `# secret-scan: allow` marker landed; `secret_scan.py --all` clean on 213 files. REQ-6 (unsatisfiable lint gate) RESOLVED via per-file-ignores, not exclusion | y | See the REQ-6 decision recorded in escalations.
- [2026-09-03T00:20:00Z] S2 | ORCH | Legacy app/ 354 passed / 1 skipped, unchanged. Security 63 passed / 41 xfail. Secret scan clean on working tree and full git history. | y |

## Sprint 3 dispatch — [2026-09-03T00:25:00Z]
- [2026-09-03T00:25:00Z] S3 | L3a | Execution gate (full CHECK_ORDER, E4 kill switch last, E5 never resize, E9 TOCTOU, single-use authorization) + MockBroker + BrokerContextProvider + AlpacaPaperBroker + read-only reconciliation | dispatched | DoD: invariants 06, 07, 14 turn PASS. Owns mizan/execution, mizan/adapters, tests/execution, tests/adapters.
- [2026-09-03T00:25:00Z] S3 | L3b | SDK (evaluate / protected / execute / decision replay / verify_chain, structurally tenant-scoped) + /v1 REST surface with auth, no-IDOR, generic errors, rate limits, security headers | dispatched | DoD: invariants 11, 12 turn PASS. Owns mizan/sdk, mizan/api, tests/sdk, tests/api. Mocks the execution gate (L3a in flight) rather than waiting, per §5.4.
- [2026-09-03T00:25:00Z] S3 | L4 | Console: decision feed, decision detail, audit timeline, policy diff, kill switch + graduated-level indicator; every agent/model/broker string escaped (F-8) | dispatched | Owns mizan/console, tests/console. Decoupled by design — takes a `client`, so not blocked on L3b.
- [2026-09-03T00:25:00Z] S3 | L0/L2b joint | Close REQ-2 (23 remaining redact() key/structure gaps, NFKC+casefold, header pair-lists, secrets in string values) and REQ-3 (delete the STRUCTURAL_SECTION_KEYS workaround by fixing _is_contract_object) | dispatched | Orchestrator granted joint write access to mizan/contracts/canonical.py + mizan/audit + tests/audit for this task only: the fix and the strict xfails that pin it sit on opposite sides of a lane boundary, so splitting it would make one half necessarily red. contracts/ and tests/invariants/ remain frozen and untouched; zero schema diff is a DoD item.
- [2026-09-03T00:25:00Z] S3 | L5 | NOT dispatched this wave, by design | n/a | Sweeps 6 (gate bypass) and 7 (adversarial agent input) are mandated to run AFTER every L2/L3 merge, and L3's gate does not exist until this sprint lands. Running them now would sweep a NotImplementedError. L5 is first in the S3 checkpoint wave, against real code. This is sequencing, not idleness.
- [2026-09-03T00:25:00Z] S3 | L6 | NOT dispatched this wave, by design | n/a | Integration tests and the killer demo need the SDK and the gate that L3a/L3b are writing now. L6 runs at the S3 checkpoint alongside L5.

## Sprint 3 checkpoint — [2026-09-03T01:45:00Z]
- [2026-09-03T01:45:00Z] S3 | L3a | Execution gate (full CHECK_ORDER), MockBroker, BrokerContextProvider, AlpacaPaperBroker, read-only Reconciler | y | 169 tests. Fixed a real BLOCKING defect: ExecutionResult.reason_codes must be sorted+deduped or a triple-code block raised ValidationError instead of blocking. Alpaca paper-host check was containment, not equality — `paper-api.alpaca.markets.example.invalid` would have passed; now host equality via urlsplit (F-19).
- [2026-09-03T01:45:00Z] S3 | L3b | SDK (evaluate/protected/execute/decision replay/verify_chain, structural tenant scoping) + /v1 REST surface + api/hardening.py | y | 260 tests. Three real defects fixed: (1) every route taking `Request` was DEAD — FastAPI imported inside create_app under `from __future__ import annotations` resolved the annotation to a required query param, so evaluate/kill-switch/health all 422'd before their first line; the API had never been smoke-tested. (2) @protected submitted the UN-REDUCED order — on a REDUCE the developer's own submit would have sent the original size. (3) @protected with dry_run=False double-submitted.
- [2026-09-03T01:45:00Z] S3 | L4 | Console: six views, escaping as the hard requirement | y | 145 tests. Single sink `el()` refuses script/style/iframe/on*/srcdoc; text neutralised for bidi/zero-width then escaped, and deliberately NOT NFKC-normalised (normalising a fullwidth homoglyph payload would manufacture real markup). NotFound collapsed before any view sees it, so unknown id and another tenant's id are indistinguishable. Declined to ship a Streamlit app: its only route to custom markup is the exact `unsafe_allow_html` sink F-8 was raised about.
- [2026-09-03T01:45:00Z] S3 | L0/L2b joint | REQ-2 (23 redact gaps) and REQ-3 (STRUCTURAL_SECTION_KEYS workaround deleted) both CLOSED | y | Strict xfails 24 → 0. REQ-3 fixed properly: `_contract_field_index()` reflects over every ContractModel so `Policy.authorization` is recognised from the models' own declarations, not from schema_version. Caught a latent record-breaking bug the request did not mention (CalendarState.session).
- [2026-09-03T01:45:00Z] S3 | GATE | **Verified in a CLEAN CLONE, not in the working tree.** HEAD e16004a: 1929 passed, 1 skipped, 41 xfailed, 0 failed. Invariants 18/18 PASS, PENDING-IMPL=0, BLOCKING=0 — the first fully green invariant suite of the build. ruff clean. secret-scan clean over 266 files. generate_schemas --check: 9 up to date, freeze intact. examples/killer_demo.py runs all seven beats. | y |
- [2026-09-03T01:45:00Z] S3 | GATE | Clean clone of 8cdb610 is RED (24 failures) — recorded as evidence for ESC-3, not as an open defect: e16004a healed it and HEAD is green. | y | This is why checkpoint verification moved into a clean clone.
- [2026-09-03T01:45:00Z] S3 | ORCH | mizan-04 stood down; this session holds the tree as sole writer. Its Sprint 1-2 lanes are recorded in its handover: S1 L0-A/L0-B/L0-C/L5 (all four died on one shared rate limit), S2 L1/L2a/L2b/L0-finish/L3-combined (all completed). Its combined L3 dispatch was the collision source. | y | See ESC-3.

## Sprint 3 checkpoint — L6 critic & integration — [2026-09-03T07:35:00Z]
- [2026-09-03T07:35:00Z] S3 | L6 | **Freeze audit, run TWICE because HEAD moved under it. Every hunk in both ranges accounted for; none weakening** | y | **Range 1, `9eebf24..6b1a800`:** exactly four changed files across two commits, no file added to or removed from either frozen tree, no `*.schema.json` / `reason_codes.json` / `error_codes.json` byte changed. Two hunks were known (ESC-1's hypothesis bound in test_01; the B905 `strict=False` in test_10) and both were RE-VERIFIED rather than accepted — `AdvisoryOpinion(recommendation="REDUCE", recommended_quantity="0")` really does raise, so ESC-1 removed only unconstructible inputs; `strict=False` really is `zip`'s default. Two were UNRECORDED: `contracts/CANONICAL.md` (233 lines) and `contracts/README.md` (76), documentation added to a frozen DIRECTORY in a5aab5b — behaviour-preserving, and CANONICAL.md's checkable claims are now pinned by L6 tests that fail if the code drifts from the published spec. **Range 2, `6b1a800..f30f119`,** which landed mid-audit: nine files, 552 insertions, one deletion (a replaced docstring line), all ADDITIVE or STRENGTHENING — two new tests appended to test_01 (one of which pins the very property L6 had verified by probe), a new `opinion()` precondition that can mask nothing because the contract's refusal is asserted separately, four appended fixture helpers redefining nothing, five rows added to the conftest registry with the classification logic untouched, and invariants 19–23. Full account in ledger/escalations.md at [2026-09-03T07:10:00Z], [07:20:00Z] and the [07:30:00Z] correction. **Lesson for the next auditor: `git diff <commit> HEAD` on a frozen path is not sufficient — L6 caught a live uncommitted frozen-file edit only by also running `git status --porcelain` and `git diff HEAD`, and caught a whole new commit only by re-checking HEAD at the end.**
- [2026-09-03T07:35:00Z] S3 | L6 | **Invariant-weakening hunt across the whole suite: NONE FOUND** | y | Zero `xfail` outside `tests/security` (whose xfails document legacy behaviour and L5's own open findings, each naming the F-number that removes it); zero `skip`/`skipif` beyond three environment guards and one legacy-database guard; zero `approx`/tolerance/`isclose` anywhere in the tree; zero trivially-true assertions; four `pytest.raises(Exception)`, each with a `noqa: B017` and a stated reason, one of them L6's own. Exactly one file deleted under `tests/` since the freeze (`tests/execution/conftest.py`, documented in ESC-3) and it contained zero test functions. The 24 `xfail(strict=True)` gap tests in `tests/audit/test_redaction.py` were traced individually from their 48969ed form to HEAD: **every one is STRONGER.** The key sweep now asserts with a deliberately NON-credential-shaped value, so it proves the KEY rule instead of passing by accident on the value scrubber — the single best decision in the rework, and the pattern other lanes should copy. The header pair-list test asserts the resulting structure rather than a substring absence; the free-text test additionally asserts the prose survives and the chain still verifies. Two coverage relocations noted, both behaviours re-verified by direct probe.
- [2026-09-03T07:35:00Z] S3 | L6 | **tests/integration: 64 tests, 9 modules, green** | y | Whole-pipeline coverage through the shipped public entry points: an APPROVE that executes; a REDUCE where the size at the broker is the reduced one (via `Mizan.execute` AND via `@protected`, pinning the defect L3b fixed); a REJECT that reaches no venue on any of three routes; an authorization expiring before AND during the gate; the kill switch asserted as an ORDERING fact — the switch and the broker share one call log, so E4 is proved by position in the sequence rather than by outcome — plus a switch thrown mid-gate; TOCTOU degradation, fresh rejection, response-level escalation and policy rebind with E5 pinned; cross-tenant reads, replays, executes and agent impersonation refused through the SDK and through the real `/v1` HTTP surface with real bearer tokens and an on-disk per-tenant SQLite boundary; an evidence pack whose verifier is `contracts/CANONICAL.md` §6 TRANSCRIBED, importing only `hashlib` and `json`, run against good, tampered, deleted and reordered chains; and the demo checked bullet by bullet against CURRENT_AIM.md. Policies are loaded from YAML documents through the real loader and validator; ledgers are real SQLite files. **MOCKS: exactly one, declared in `tests/integration/_world.py`** — `mizan.adapters.MockBroker`, a shipped adapter, standing in for the venue because the alternative is a credentialed network call this build forbids. Nothing else is mocked, patched or monkeypatched, and the suite imports nothing from `tests/fixtures`, `tests/invariants` or `tests/security`, so no other lane's edit can re-point what it asserts.
- [2026-09-03T07:35:00Z] S3 | L6 | **One new defect found: ESC-4 (routed as REQ-22)** | y | `duplicate_order`, and the quantity arm of `erroneous_order`, are enabled, configured `blocking`, report `passed=True, severity="blocking", detail=""`, and CANNOT FAIL: their only input, `RiskContext.recent_orders`, is structurally empty in every shipped pipeline. Reproduced end to end — two proposals differing only in `created_at`, same symbol/side/quantity, one second apart, both APPROVED with empty reason codes and both SUBMITTED to the venue; the gate's idempotency step does not catch it either, because `idempotency_key` derives from `proposal_id`. Pinned by four tests written to FAIL when it is fixed, each naming ESC-4 in its assertion message. Six further findings filed: REQ-20 (a broker outage leaves no record at all), REQ-21 (CHECK_ORDER step 4 is the only broker-touching step with no failure handler), REQ-23 (the console tells the operator a `policy_activated` event is recorded; nothing records one), REQ-24 (the default kill switch is process-local, so the `/v1` flip stops one worker), REQ-25 (three gaps between the demo and the aim), REQ-26 (L5's sweep-7 fixture literals, since closed by L5 itself — follow-up recorded). L6 independently corroborated L5's F-33 and corrected one of its own test names that had overstated the refusal.
- [2026-09-03T07:35:00Z] S3 | L6 | **Killer demo verified against CURRENT_AIM.md, bullet by bullet** | y | Runs clean; byte-for-byte deterministic across runs. PROVED: versioned policy, and a policy change flipping the same inputs to REJECT; reason codes on refusal; deterministic replay to an identical verdict hash; the kill switch at the mutation boundary; prompt-injection immunity, structurally — the two verdict hashes are equal, so it is not a filter that could miss a phrasing. NOT PROVED: REDUCE — the demo shows only APPROVE and REJECT; append-only at the STORAGE layer — it runs on `InMemoryLedger`, so the SQLite triggers are never exercised in front of the reader; Alpaca PAPER — it runs on `MockBroker` and only documents the swap. The ~10-line claim is proved by `examples/tradingagents_ten_lines.py` (the block really is ten lines) but against a local stand-in class, because the TradingAgents licence is still unverified. Every judgement above is a passing assertion in `tests/integration/test_the_demo_proves_the_aim.py`, including negatives that fail if a gap is later closed. Filed as REQ-25.
- [2026-09-03T07:35:00Z] S3 | L6 GATE | Invariants **PASS=23 PENDING-IMPL=0 BLOCKING=0 NOT-RUN=0 (of 23)**, 82 tests; full suite **2263 passed, 1 skipped, 56 xfailed, 0 failed**; `tests/integration` 64 passed; `ruff check tests/integration` clean; `secret_scan.py --all` **clean over 290 files**; `generate_schemas.py --check` 9 up to date; `git status --porcelain contracts/ tests/invariants/` empty | y | Measured at HEAD `f30f119` on a settled tree. Earlier runs were taken while L5's five sweep-6/7 modules and the invariant 19–23 commit were landing, and those numbers moved under the run — recorded as a BLOCKER at [2026-09-03T07:15:00Z]: **a checkpoint that certifies totals needs a quiet tree, and L5 and L6 were dispatched into the same wave by design.** The baseline this replaces (1929/1/41/0) plus L6's 64 accounts for 1993; the remainder is L5's sweeps and the new invariants. L6 wrote only `tests/integration/` and `ledger/`, committed nothing, staged nothing, and touched no history.

## M2 PROVEN — [2026-09-03T02:10:00Z]
- [2026-09-03T02:10:00Z] S3 | GATE | **Cross-machine decision determinism PROVEN on HEAD `5dd1604`** (workflow run 33705743438, 1m48s): `replay` green on ubuntu-latest / macos-latest / windows-latest x Python 3.12 / 3.13 - x86 and ARM - plus the `all runners agree` job. The compare job parses each artifact's JSON and string-compares the `fingerprint` VALUE; it never hashes or byte-compares archives, so differing artifact digests (Windows CRLF in the zip) are irrelevant. Each cell independently ran `--check` three times first: normal, hostile environment (PYTHONHASHSEED=0, TZ=Pacific/Kiritimati, LC_ALL=C) and randomised hash seed. | y | M2 is a permanent CI gate, not a claim. Any determinism statement must carry its SHA: the earlier 6/6 validated `ea65e188@6a68946` and was already stale when quoted.
- [2026-09-03T02:10:00Z] S3 | ORCH | The `ci` workflow (separate from `determinism`) fails at the same SHAs on two jobs: `typecheck` (mypy cannot see members of the dynamically-created `StrEnum` in mizan/contracts/errors.py and reason_codes.py - a pre-existing typing limitation in a FROZEN path, flagged non-blocking in Sprint 1 and since flipped to blocking) and `postgres-ddl`. Neither is the determinism matrix and neither affects M2. | n | Not yet triaged; recorded so the red is visible rather than latent.
- [2026-09-03T02:10:00Z] S3 | ORCH | REQ-34 CLOSED (accepted by the human): `MarketSnapshot.snapshot_id` is content-derived and `as_of` is taken from the freshest quote rather than the caller's clock. Both were required - deriving the id while `as_of` stayed `now` would have put read time inside the content and left every id unique. F-29 closed: attaching a stale id to moved quotes is now refused outright. Verified by probe, not assumption. | y | 2310 passed, 0 failed; invariants 26/26 BLOCKING=0; new fingerprint af33b293 re-verified across 4 hostile environments.
