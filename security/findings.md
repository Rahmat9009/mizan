# Security findings (append-only)

Format: `[UTC timestamp] F-n | severity (CRITICAL/HIGH/MEDIUM/LOW) | sweep # | owning lane | finding | location | status (OPEN/FIXED/ACCEPTED)`

Routing: CRITICAL halts the owning lane's merges until fixed. HIGH must be fixed within one sprint. MEDIUM before the milestone. LOW logged.

[2026-09-02T14:56:00Z] F-1 | CRITICAL | 2 | LEGACY(app/) — L1 mizan/risk and L3 mizan/adapters must not repeat | Gate bypass by price poisoning: the caller-supplied `TradeProposal.estimated_price` is the ONLY valuation price; every notional rule (buying_power, max_trade_size, position_concentration) and the execution-time `_fresh_risk_check` compute notional from it and no quote is ever fetched. 1,000 AAPL at a claimed $0.01 on a $10k-equity/$5k-buying-power account is not blocked, the governor APPROVEs 1,000 and the execution gate AUTHORIZEs 1,000 (pinned: tests/security/test_legacy_input_poisoning.py::test_legacy_price_poisoning_passes_every_notional_check, ::test_legacy_price_poisoning_is_approved_and_authorized_end_to_end). New core: notional only from `RiskContext.market_snapshot` quotes sourced by `BrokerContextProvider`; `legs[].limit_price` is a bound, never the valuation; `erroneous_order` must compare limit vs quote (E2, E9). | app/models.py:41,47-49; app/risk_engine.py:64-113; app/execution/service.py:383-384 | OPEN
[2026-09-02T14:56:00Z] F-2 | CRITICAL | 2 | LEGACY(app/) — L1 mizan/risk and L3 mizan/adapters must not repeat | Gate bypass by market-risk poisoning: `MarketRiskSnapshot` (annualized_volatility, max_drawdown_30d, liquidity_score) is accepted from the unauthenticated caller in POST /proposals/evaluate and is the sole input to the BLOCK-severity liquidity rule and the volatility haircut; claiming liquidity_score=1.0 removes the block (pinned: tests/security/test_legacy_input_poisoning.py::test_legacy_market_risk_poisoning_bypasses_liquidity_block, ::test_legacy_volatility_haircut_is_caller_controlled). New core: market data enters only through the context provider; the SDK/API must not accept a caller-built MarketSnapshot for a decision that will execute. | app/api.py:22-27,125-127; app/market_risk.py:14-23; app/risk_engine.py:122-144 | OPEN
[2026-09-02T14:56:00Z] F-3 | HIGH | 2 | LEGACY(app/) — L3 mizan/api must not repeat | No authentication or authorization on any route, including state-changing POST /proposals/{id}/execute and POST /orders/{id}/reconcile; CORS allow_credentials=True and an Authorization header is allowed but never read; anyone reaching the port triggers paper order submission for any evaluated proposal and reads the whole account (pinned: tests/security/test_legacy_api_disclosure.py::test_execute_route_is_reachable_without_any_credential, ::test_reconcile_route_is_reachable_without_any_credential, ::test_anonymous_callers_receive_positions_and_buying_power). New core: bearer token per agent → tenant binding on every /v1 route; agent identity never taken from the body. | app/api.py:65-71,104-156 | OPEN
[2026-09-02T14:56:00Z] F-4 | HIGH | 2 | LEGACY(app/) — L3 mizan/execution must not repeat | Kill-switch bypass by staleness (E4): `ExecutionConfig.from_environment()` is called once in `BackendServices.__init__` and frozen; `ExecutionGate._configuration_gate` re-checks the frozen value, so flipping ALPACA_EXECUTION_KILL_SWITCH on a running process has no effect and there is no runtime control endpoint (pinned: tests/security/test_legacy_input_poisoning.py::test_legacy_kill_switch_is_frozen_at_config_load). New core: `KillSwitch.is_active()` read on every call, as the LAST check before the mutation; flips recorded as hash-chained ControlEvents. | app/execution/gate.py:40-71,199-214; app/services.py:53 | OPEN
[2026-09-02T14:56:00Z] F-5 | HIGH | 2 | LEGACY(app/) — L2 mizan/audit and L0 infra must not repeat | Audit is not tamper-evident (A2, A5): `audit_events` has no hash chain and no BEFORE UPDATE/DELETE triggers; `execution_results` and `broker_orders` are ON CONFLICT DO UPDATE upserts; anyone with file/DB access rewrites history undetected and nothing is customer-verifiable offline. New core: one chain per tenant from ZERO_HASH, triggers RAISE(ABORT,'append-only') at the storage layer, `verify_chain` CLI. | app/persistence/schema.py:119-137; app/persistence/repositories.py:182-252 | OPEN
[2026-09-02T14:56:00Z] F-6 | HIGH | 2 | LEGACY(app/) — L0 mizan.contracts.canonical.redact must cover; L2 TenantLedger.append must apply | Recursive redaction `_AuditEventFactory._sanitize` is key-based with a closed list and no Unicode normalisation. It MISSES: proxy-authorization, cookie, set-cookie, www-authenticate, bearer, session/session_id/sessionid, jwt, auth, signature/x-signature, private_key, passphrase, passwd/pwd/pass, connection_string, dsn, database_url/db_url, response_headers/http_headers as collections (inner Set-Cookie survives), header pair-lists [("Authorization", ...)], secrets embedded in string VALUES (Bearer ..., ?api_key=...), nested pydantic models, sets (unsanitised and unserialisable), Cyrillic/fullwidth homoglyph keys, account_id/account_number/ssn — 41 gaps pinned as xfail(strict=True) in tests/security/test_legacy_redaction.py. It DOES cover api_key (any case/separator), *secret*, Authorization/authorizationHeader, *token, password, credential(s), header/headers/request_headers, lists/tuples/deep nesting (50 passing pins). New redact(): NFKC + casefold keys; treat *authorization*, *cookie*, *session*, jwt, *signature*, private_key, passphrase, dsn, connection_string, database_url, passwd as sensitive; wholesale-redact any *header* collection incl. pair-lists; add value patterns (Bearer, sk-, sk-ant-, Alpaca PK/AK, rc_, URL ?api_key=); recurse into pydantic models and sets. | app/audit.py:67-107 | OPEN
[2026-09-02T14:56:00Z] F-7 | HIGH | 2 | LEGACY(app/) — L2 mizan/audit must not repeat | Redaction is applied to only 1 of 4 audit entry points: `append_execution` sanitises; `append_risk`, `append_ai_risk` (LLM output verbatim) and `append_governor` persist `model_dump()` unredacted (pinned: tests/security/test_legacy_redaction.py::test_legacy_only_execution_events_pass_through_sanitize). New core: `TenantLedger.append` applies `redact` to the WHOLE DecisionRecord before hashing, not per event type. | app/audit.py:26-51 vs 53-65 | OPEN
[2026-09-02T14:56:00Z] F-8 | MEDIUM | 2 | LEGACY(app/, ui/) — L4 mizan/console must not repeat | Stored HTML injection / XSS in the operator UI: LLM-authored `reasoning` is joined verbatim into `GovernorDecision.reason` (app/governor.py:53,79), persisted, served by the API and rendered by ui/streamlit_app.py inside `st.markdown(..., unsafe_allow_html=True)`; risk-check `message` and audit payload message/reason/risk_thesis hit the same sink. A prompt-injected advisory response therefore controls markup in the console (pinned: tests/security/test_legacy_api_disclosure.py::test_llm_reasoning_flows_verbatim_into_governor_reason, ::test_streamlit_renders_tainted_fields_with_unsafe_allow_html). The Streamlit UI is disposable (C11) but the console must escape every agent/model/broker string and never use unsafe_allow_html with tainted data. | app/governor.py:53,79; ui/streamlit_app.py:420-429,486-491,699-705 | OPEN
[2026-09-02T14:56:00Z] F-9 | MEDIUM | 2 | LEGACY(app/) — L2 mizan/authorization and L3 mizan/execution must not repeat | Race on execute: no local single-use authorization or lock; two concurrent POST /proposals/{id}/execute both pass every gate and both call `submit_market_order`; duplicate suppression is delegated entirely to Alpaca rejecting a reused client_order_id plus the post-failure reconcile (pinned: tests/security/test_legacy_api_disclosure.py::test_concurrent_executes_both_reach_submit_when_broker_does_not_dedupe). New core: `AuthorizationRegistry.consume(auth_id)` atomic, exactly once, at gate step 5, before the last freshness and kill-switch checks. | app/services.py:117-166; app/execution/service.py:145-201 | OPEN
[2026-09-02T14:56:00Z] F-10 | MEDIUM | 2 | LEGACY(app/) — L2 mizan/authorization must not repeat | Idempotency key is `sha256(proposal_id)[:40]` over a CALLER-CHOSEN string, not a canonical content hash (E7); it carries no tenant, so identical proposal ids across tenants/DB resets collide and are resolved by a symbol/side/qty mismatch check (good pattern to keep) rather than by construction. New core: `proposal_id = proposal_id_for(payload)` verified on validation; `idempotency_key` over {tenant_id, proposal_id, legs}. | app/execution/service.py:47-51; app/models.py:37 | OPEN
[2026-09-02T14:56:00Z] F-11 | MEDIUM | 2 | LEGACY(app/) — L2 mizan/authorization must not repeat | Authorization freshness defaults to 120 s and is configurable up to 3600 s (EXECUTION_MAX_DECISION_AGE_SECONDS 1..3600) versus E6 (5–30 s); authorizations are re-derived on every execute call and never single-use; `ExecutionAuthorization` has no expiry or hash of its own. New core: `ttl_seconds` 5..30, `expires_at`, `single_use: True`, `authorization_hash`, `bound_state`. | app/execution/gate.py:52-71,216-227; app/execution/models.py:52-71 | OPEN
[2026-09-02T14:56:00Z] F-12 | MEDIUM | 1 | LEGACY(app/) — L2 mizan/advisory and L0 infra must not repeat | Diagnostics go to stdout via `print()`; there is no `logging` usage anywhere in app/, so output cannot be levelled, routed or centrally redacted. With FEATHERLESS_DEBUG=true the full parsed model output (risk_thesis, reasoning, hidden_risks) is printed (pinned: tests/security/test_legacy_api_disclosure.py::test_debug_flag_prints_llm_reasoning_to_stdout); the substring redactor `_safe_debug_value` over-redacts (*token* hides usage counters) and, like F-6, never inspects values. No secret value is printed today. New core: structured `logging` with a redacting filter; model output never above DEBUG; never set OPENAI_LOG=debug in any environment that has real keys. | app/providers/featherless_risk.py:302-380; probe_featherless.py:104-137 | OPEN
[2026-09-02T14:56:00Z] F-13 | MEDIUM | 1 | L0-C (infra) — verify at Sprint-1 checkpoint | No secret scanning or pre-commit hook existed in the repo at sweep time (W0 mandates "secret scanning"); `.gitignore` ignored `.env` but not `.env.*`/`.env.local`, `*.pem`, `*.key`, `*.p12`, `secrets/`; nothing prevented the next commit from carrying a key. L0-C's `.pre-commit-config.yaml` and `.gitignore` changes appeared in the working tree mid-sweep (uncommitted); L5 re-checks in Sweep 1 next sprint and will pin the hook with a test that a fixture key is refused. | .gitignore; .pre-commit-config.yaml (new, unverified) | OPEN
[2026-09-02T14:56:00Z] F-14 | LOW | 1 | LEGACY(app/) — L3 mizan/api must not repeat | Internal exception text is forwarded to clients: `ServiceError("DATABASE_ERROR", str(exc))` and `("INVALID_MARKET_RISK", str(exc))` pass persistence/ValueError messages through; `PersistenceConflictError` names the SQLite table ("proposals already contains different data for this proposal.") and a pydantic ValidationError on the ValueError path would echo input (pinned: tests/security/test_legacy_api_disclosure.py::test_conflict_error_forwards_internal_table_name). No secret or path is reachable this way today (pinned ::test_error_bodies_never_contain_environment_secrets) and the exception-type-name discipline in the adapters is good and must be kept. New core: `MizanError.message` is generic, plus `correlation_id`; never `str(exc)` from lower layers. | app/services.py:107,109,170,176,186,204,210; app/persistence/repositories.py:118-120 | OPEN
[2026-09-02T14:56:00Z] F-15 | LOW | 1 | LEGACY(app/) — L3 mizan/api must not repeat | GET /health is unauthenticated and discloses control-plane state (execution_enabled, dry_run, kill_switch, ai_provider) and the database technology ("SQLite"); no path is disclosed (pinned: tests/security/test_legacy_api_disclosure.py::test_health_discloses_control_plane_state_to_anonymous_callers). New core: /v1/health returns liveness only to anonymous callers; flags and vendor names only to an authenticated tenant. | app/api.py:104-119 | OPEN
[2026-09-02T14:56:00Z] F-16 | LOW | 1 | LEGACY(demo scripts) — L3/L4 examples must not repeat | demo_full_lifecycle.py prints the local database path (`services.database.path`) to stdout; probe_featherless.py prints raw provider exceptions (`{exc}` of openai APIStatusError bodies) and demo_*.py print `{exc}` for broker/config errors. Only variable names, not values, are reachable today. New core examples: print type names and reason codes only. | demo_full_lifecycle.py:111,114; probe_featherless.py:137; demo_alpaca_portfolio.py:18; demo_paper_execution.py:43,62 | OPEN
[2026-09-02T14:56:00Z] F-17 | HIGH | 2 | LEGACY(app/) — L0 infra, L2 mizan/audit, L3 mizan/api must not repeat | No tenant concept anywhere (B3): one SQLite file, one broker account, no tenant_id/agent_id in any table or model, caller-chosen proposal_id is a global namespace, GET /recent lists every proposal with its portfolio snapshot (pinned: tests/security/test_legacy_api_disclosure.py::test_stored_lifecycle_carries_no_tenant_or_agent_identity). This cannot be fixed by adding a WHERE clause: the new core must use one schema/file per tenant, a `TenantId` regex that excludes path characters, and NotFound (not Forbidden) for other tenants' ids. | app/persistence/schema.py:60-138; app/models.py:27-45; app/api.py:154-156 | OPEN
[2026-09-02T14:56:00Z] F-18 | LOW | 2 | LEGACY(app/) — L3 mizan/adapters and L2 mizan/advisory must not repeat | Credential sourcing is ambiguous and co-located: `load_dotenv()` is called at six sites (create_app import time, both providers' __init__, resolve_alpaca_paper_credentials, probe, demos) and `find_dotenv` walks parent directories, so a .env outside the repo can silently supply broker keys; broker and LLM keys live in the same process as the unauthenticated HTTP API (F-3), so any SSRF/RCE in the API is a key exposure. No value is ever logged. New core: read env exactly once at adapter construction, never call load_dotenv inside library code, keep the API process key-less where the deployment allows (B2). | app/api.py:49; app/alpaca/client.py:94; app/providers/featherless_risk.py:115; app/providers/claude_risk.py:45 | OPEN
[2026-09-02T14:56:00Z] F-19 | LOW | 2 | LEGACY(app/) — L3 mizan/adapters must not repeat | `PaperExecutionAlpacaAdapter.paper_mode_verified` is a constant `True` property and the execution/reconciliation services gate on it; the real proof is the base-URL equality check in `__init__` (good, keep). Any stub returning True satisfies the Protocol. New core: `environment: Literal["paper"]` on the adapter type, base URL asserted at construction AND immediately before `submit_order`. | app/alpaca/execution.py:39-47,60-62; app/execution/service.py:155-157 | OPEN
[2026-09-02T14:56:00Z] F-20 | LOW | 2 | LEGACY(app/) — L0 contracts must not repeat | Unbounded free text: `TradeProposal.thesis`/`invalidation_condition` and LLM `reasoning`/`hidden_risks`/`risk_thesis` have no max_length; an unauthenticated caller can amplify paid LLM calls (cost DoS) and bloat the audit table. New contracts: `reasoning` ≤ 20,000 chars (already specified); cap advisory text fields and list lengths. | app/models.py:43-44,116-126; app/providers/featherless_risk.py:43-53 | OPEN
[2026-09-02T14:56:00Z] F-21 | MEDIUM | 2 | LEGACY(app/) — L1 mizan/policy and L2 mizan/audit must not repeat | Policy is unversioned and unhashed: `RiskPolicy` is a frozen dataclass with in-process defaults, any caller may construct `RiskEngine(RiskPolicy(max_trade_pct=1.0))`, and no decision or audit record references a policy id/version/hash, so A1 replay is impossible and policy tampering leaves no trace. New core: `Policy.policy_hash` verified on load, `PolicyRef` in every RiskContext/RiskEvaluation/GovernorDecision, `policy_snapshot` in the DecisionRecord, activation as a ControlEvent. | app/risk_engine.py:8-20; app/models.py:105-114,137-147 | OPEN
[2026-09-02T14:56:00Z] F-22 | MEDIUM | 2 | LEGACY(app/) — L2 mizan/advisory must not repeat | Sensitive data leaves the boundary unrecorded: on every evaluate the FULL portfolio (positions, quantities, market values, unrealized P&L, equity, cash, buying power) plus the agent thesis is serialised into the user prompt and sent to Featherless (default) or Anthropic; the prompt is not hashed or stored, so the advisory cannot be replayed or audited, and the data-sharing is not tenant-configurable. New core: minimise prompt fields, record `prompt_hash` and `raw_hash`, make advisory enablement and the vendor a per-tenant policy choice, document vendor data terms. | app/providers/featherless_risk.py:408-425; app/providers/claude_risk.py:194-212 | OPEN
[2026-09-02T14:56:00Z] F-23 | LOW | 1 | L0 (record) | Sweep 1 negative result, recorded for the ledger: NO secret exists anywhere in git history. All 4 commits on all refs (main, origin/master; b238f60, 741f953, c549b2a, 13dde95) plus every loose/packed blob (`git fsck --unreachable`: none; no stash) were scanned for Alpaca PK/AK, OpenAI sk-, Anthropic sk-ant-, Featherless rc_, AWS AKIA, GitHub ghp_/gho_, Slack xox, Google AIza, PEM blocks, JWTs, connection strings and literal KEY/SECRET/TOKEN/PASSWORD assignments; only variable names, empty placeholders and test fixture strings appear. `.env` was never tracked; `.env.example` is placeholder-only; the remote URL carries no credential; no NEXT_PUBLIC_/VITE_ variables exist. Nothing to rotate. | git history; .env.example; .gitignore | ACCEPTED
[2026-09-02T15:05:00Z] F-24 | LOW | 1 | L0-C (infra) | L0-C's new `scripts/secret_scan.py` (uncommitted, appeared mid-sweep) is sound: `--self-test` passes 59 cases, `--history` independently reports all 4 commits clean, and `--paths` catches high-entropy synthetic fixtures shaped like OpenAI sk-, Anthropic sk-ant-, Featherless rc_, Alpaca secret, a DSN with an embedded password and a PEM block (low-entropy fixtures are deliberately ignored). Two gaps: (1) no `.secretscan-allow` exists and the scanner flags its own self-test literals (`scripts/secret_scan.py:734-735`, 4 findings, exit 1 in `--all` and `--paths`), so the `--staged` pre-commit hook will refuse the commit that adds the scanner itself unless an allow-list entry is added or the fixtures are built at runtime from fragments; (2) the hook is opt-in (`python scripts/install_hooks.py` / pre-commit) and nothing in CI is verified yet because the workflow is uncommitted. `.env.example` (expanded by L0-C) remains placeholder-only; the commented `postgresql://user:pass@...` example passes the scanner. | scripts/secret_scan.py:734-735; (absent) .secretscan-allow; .pre-commit-config.yaml | OPEN

[2026-09-02T23:08:48Z] F-24 UPDATE | LOW | 1 | ORCH | Both gaps closed by the Orchestrator at the Sprint-1 checkpoint: the scanner's own self-test literals now carry inline `# secret-scan: allow` markers (precise, rather than allow-listing the whole file, so a real secret committed there would still be caught), and `--all`, `--history` and `--self-test` are all clean. The pre-commit hook remains opt-in via scripts/install_hooks.py; CI enforcement is verified by tests/infra/test_ci_workflow.py this sprint. | scripts/secret_scan.py | FIXED
[2026-09-02T23:08:48Z] F-25 | INFO | ORCH | ORCH triage | Routing of the Sweep 1-2 findings: F-1/F-2 (CRITICAL, valuation and market-risk inputs must come from context snapshots, never from the proposing agent) relayed to L1 mid-flight with a required regression test. F-5/F-6/F-7 (HIGH, redaction before hashing, applied to the whole record, and the 41 known redact gaps) relayed to L2b. F-3/F-14/F-15 (API auth, error disclosure, health disclosure) and F-4/F-9/F-19 (kill-switch staleness, execute race, paper proof) are L3's and will be in its brief. F-8 (console XSS) is L4's. F-17/F-21/F-22 are already answered by the frozen contracts (tenant isolation, PolicyRef + policy_snapshot, prompt_hash/raw_hash). F-23 (no secret in git history) ACCEPTED, nothing to rotate. No CRITICAL or HIGH finding is open against NEW core code; every one is a legacy finding carrying a do-not-repeat instruction. | security/findings.md | ROUTED
[2026-09-02T23:51:59Z] F-6 UPDATE | HIGH | 2 | L0 (canonical.redact) | CLOSED. Redaction rebuilt: (1) sensitive key + mapping/list now RECURSES with inherited sensitivity instead of replacing wholesale, which both preserves contract sub-sections that merely share a sensitive name (Policy.authorization is a TTL section, not a header) and still redacts strings inside a credentials blob; (2) header/cookie bags remain wholesale-replaced, including pair-lists, because header names are themselves revealing; (3) 15 new key spellings (pwd, passphrase, www_authenticate, bearer, session*, jwt, signature, private_key, database_url, db_url, account_id, account_number, ssn); (4) NFKC + an explicit Cyrillic/Greek homoglyph table, so a key that reads as "api_key" to a human is treated as one; (5) NEW value-pattern scrubbing over every string under any key (Bearer, sk-, sk-ant-, rc_, Alpaca PK/AK, AKIA, ghp_, xox*, JWT, PEM, scheme://user:pass@host, ?api_key=), replacing only the matched span so surrounding prose stays auditable. That last one closes the gap that mattered most: every contract model is extra="forbid", so a credential can only reach a record pasted into free text, where key-based redaction is blind. One documented hole: a numeric value survives under a sensitive key that contains ":" or "/", so typed decimal maps keyed by data (exposure_by_signal_source: {"vendor:secret-feed": "100.5"}) are not destroyed; a plain field name has no separator, so account_number and ssn are still redacted. | mizan/contracts/canonical.py | FIXED
[2026-09-02T23:51:59Z] F-7 UPDATE | HIGH | 2 | L2b | CLOSED by L2b: redact is applied once to the WHOLE record payload before the hash is computed, so stored bytes and audit_hash cover the same redacted content and an external verifier agrees without ever seeing a credential. Not per event type, unlike the legacy code. | mizan/audit | FIXED
[2026-09-02T23:51:59Z] F-5 UPDATE | HIGH | 2 | L2b | CLOSED by L2b: per-tenant SQLite files with BEFORE UPDATE / BEFORE DELETE / sequence / link / tenant triggers, and an AST test asserting no SQL the ledger runs is an upsert. | mizan/audit | FIXED
[2026-09-03T00:12:52Z] F-26 | MEDIUM | ORCH | L0 (canonical.redact) | FIXED same session. The Alpaca key-id value pattern `\b(?:PK|AK)[A-Z0-9]{16,}\b` also matched OCC option symbols for every underlying beginning AK or PK — AKAM260925C00230000 (Akamai), PKG260925P00120000 (Packaging Corp). Not a cosmetic false positive: occ_symbol is a contract-validated field, so redacting it makes the DecisionRecord unbuildable and NO option on those underlyings could be recorded at all. It fails closed, so nothing leaked and nothing was silently wrong, but it was an availability outage scoped to two-plus real tickers. Found by L2b in review, not by any test. Fixed with a negative lookahead for the OCC shape (ROOT + YYMMDD + C/P + 8 digits), which an API key never has; regression test in tests/contracts asserts both directions. | mizan/contracts/canonical.py | FIXED

<!-- Sweep 6 (gate bypass) and Sweep 7 (adversarial agent input), run 2026-09-03 against the NEW core
     after the L2/L3/L4 merge. The first run against real code rather than stubs. -->

[2026-09-03T07:30:00Z] F-27 | HIGH | 7 | L0 (mizan/contracts/canonical.py) | **Third redaction collision: an adjusted OCC option symbol is destroyed, and one such position takes the whole tenant's ledger down.** F-26 taught the OCC lesson and its fix is a negative lookahead of shape `[A-Z]{1,6}\d{6}[CP]\d{8}` inside the Alpaca key-id value pattern. That lookahead only recognises a *purely alphabetic* OCC root. After a corporate action the OCC assigns an ADJUSTED root carrying a numeric suffix - AKAM1, AKAM2, PKG1, AKRO1, PKOH1 - which the contract's own `OCC_SYMBOL_PATTERN` (`^[A-Z][A-Z0-9]{0,5}\d{6}[CP]\d{8}$`) accepts and which `AlpacaPaperBroker._position_of` copies verbatim from the broker into `Position.occ_symbol`. Those symbols still match `(?:PK|AK)[A-Z0-9]{16,}` and are replaced with `[REDACTED]`, which is not a valid `OccSymbol`, so `DecisionRecord.build(**redact_for_persistence(payload))` refuses the record. This is WIDER than F-26: the portfolio snapshot is inside `risk_context` on EVERY decision record, so a single held adjusted-option position on any AK*/PK* underlying stops the tenant recording ANY decision, equity decisions included - `Mizan.evaluate` raises and nothing is chained. Fails closed (no leak, no silently-wrong record) but it is a total governance outage for that tenant, triggered by ordinary broker data with no attacker involved. Fix: replace the lookahead body with the contract's own OCC pattern body (`[A-Z][A-Z0-9]{0,5}\d{6}[CP]\d{8}`) so the exemption and the contract agree by construction rather than by two hand-written regexes drifting apart. Pinned: tests/security/test_sweep7_redaction.py::test_f27_an_adjusted_occ_symbol_must_survive_redaction (5 roots), ::test_f27_one_adjusted_option_position_must_not_stop_the_tenant_recording_anything (both xfail(strict=False) - remove the markers with the fix), plus ::test_f27_the_failure_is_closed_not_silent and ::test_f27_an_adjusted_occ_root_is_a_valid_contract_value which stay green either way. | mizan/contracts/canonical.py:88-92 (SENSITIVE_VALUE_PATTERNS, the Alpaca key-id pattern) | OPEN

[2026-09-03T07:30:00Z] F-28 | HIGH | 6 | L3 (mizan/sdk, mizan/authorization) | **Single-use authorization is process-local, and the registry that enforces it cannot be injected, so a multi-worker deployment submits one authorization twice.** `mizan/authorization` says the registry "is the mechanism that makes 'single use' true under concurrency rather than merely documented", and within one process it is: `InMemoryAuthorizationRegistry.consume` is a locked test-and-set and two racing threads produce exactly one submission. But `Mizan.__init__` hard-wires `self.registry = _authorization.InMemoryAuthorizationRegistry()` and takes NO `registry` parameter, while every other collaborator - broker, ledger, advisory, kill_switch, config, clock - is injectable. `create_app` documents the resolver form ("what multi-tenant deployments use") and any uvicorn/gunicorn worker count above one gives each worker its own registry over a shared `SqliteLedger`. DEMONSTRATED: two `Mizan` instances over one ledger and one broker, interleaved so both complete `broker.find_order` before either submits, produce TWO `SUBMITTED` results and TWO identical orders with the same `client_order_id`. Sequentially the second instance also re-consumes the authorization (`registry.was_consumed` is False on the peer) and only the broker's memory of the client order id turns the replay into `RECONCILED_EXISTING` - which is precisely the posture F-9 faulted the legacy build for ("duplicate suppression is delegated entirely to Alpaca rejecting a reused client_order_id"). Not rated CRITICAL because the single-process default is correct and a real venue rejects a duplicate `client_order_id`, but the exactly-once claim is a product claim and today it holds only for one process. Fix: add `registry: AuthorizationRegistry | None = None` to `Mizan.__init__` and ship a durable implementation (a row per `auth_id` in the tenant's SQLite/Postgres unit, consumed with an INSERT that fails on the unique key - the atomicity then belongs to the database, not to a Python lock). Related: ledger/requests.md REQ-24 records the same shape of defect for the kill switch. Pinned: tests/security/test_sweep6_gate_bypass.py::test_f28_the_authorization_registry_cannot_be_injected_into_mizan, ::test_f28_two_pipelines_over_one_ledger_must_not_submit_one_authorization_twice (xfail(strict=False)), ::test_f28_the_broker_idempotency_read_is_the_only_remaining_defence. | mizan/sdk/__init__.py:98 (`self.registry = ...`, no parameter); mizan/authorization/__init__.py:293-316 | OPEN

[2026-09-03T07:30:00Z] F-29 | MEDIUM | 6 | L3 (mizan/execution) + L0 (contracts, FROZEN) | **The market half of the state binding is compared by identity, so `RevalidationReport.state_changed` is wrong in both directions.** `ExecutionGate._state_changed` says in its own docstring "Compared by hash, not by identity: a snapshot that kept its id but changed its contents is a changed state, and that is precisely the case an attacker would arrange." That is true for the portfolio (`bound.portfolio_state_hash != object_hash(portfolio)`) and FALSE for the market: `BoundState` has `market_snapshot_id` and no `market_state_hash`, so the only market comparison is `bound.market_snapshot_id != market.snapshot_id`. DEMONSTRATED: an order is SUBMITTED with `revalidation.state_changed=False` while the quote it was authorized against moved from 228.50 to 230.00 under an unchanged `snapshot_id`. The converse is just as bad in production: `AlpacaPaperBroker` derives `snapshot_id` as `f"alpaca-mkt-{format_ts(as_of)}"`, a microsecond-resolution timestamp, so the id changes on EVERY read and `state_changed` is True on every real execution whether or not anything moved. Either way the field carries no information, and it is the field an auditor or the console reads to learn whether the world moved between authorization and submission. NOT a size bypass - the TOCTOU step re-runs the engine against the fresh quotes, and a move that no longer supports the size still blocks (pinned) - so this is audit fidelity, which is the product. Fix, without touching the frozen contract: make `snapshot_id` content-derived in the adapters exactly as `BrokerContextProvider` already does for `context_id` (`ctx-<sha256[:32]>`), so identity and content coincide; the complete fix (a `market_state_hash` on `BoundState`) needs a contract change and therefore an escalation. Pinned: tests/security/test_sweep6_state_binding.py::test_f29_bound_state_carries_no_market_state_hash, ::test_f29_a_market_that_moved_under_a_reused_snapshot_id_must_report_state_changed (xfail(strict=False)), ::test_f29_the_price_move_is_still_re_evaluated_even_though_it_is_not_reported, ::test_the_portfolio_half_of_the_binding_is_compared_by_content_hash. | mizan/execution/__init__.py:418-437 (`_state_changed`); mizan/contracts/execution_authorization.py:86-96 (`BoundState`); mizan/adapters/alpaca_paper.py:211,247 | OPEN

[2026-09-03T07:30:00Z] F-30 | HIGH | 7 | L1 (mizan/risk) | **An OPENING short order switches off every capital and concentration check, because "sell" is read as "reduces risk" regardless of intent or holdings.** `valuation.exposure_of` sums `signed_quantity(leg) x price x multiplier` into `Exposure.change`, so any sell leg makes `change` negative and `increases_risk` False. Then: `buying_power_sufficiency` and `buying_power_utilization` return early on `exposure.change <= ZERO`, and `concentration_limit`, `sector_concentration`, `aggregate_exposure`, `model_provider_concentration`, `signal_source_concentration` and the book-gross check return early on `not exposure.increases_risk`. DEMONSTRATED: a naked short call with `intent="open"` on a symbol the account holds NOTHING of records `buying_power_sufficiency`, `buying_power_utilization`, `concentration_limit` and `sector_concentration` as PASS with details "the order does not consume buying power" and "the order does not increase symbol exposure", while the identical LONG order is measured by all four (the long is caught by CONCENTRATION_LIMIT_EXCEEDED at 200 contracts; the short is not). A short position IS exposure and consumes margin; `change < 0` conflates "reduces an existing long" with "creates a new short". The engine already has everything it needs to tell them apart - `proposal.intent` and `portfolio.positions` - and uses neither. Same class as F-1 (a valuation that understates risk bypasses every rule expressed in money), reappearing in the new core. Also note `Exposure.gross` for an option leg is the PREMIUM (`quantity x mark x 100`), so even where a check does run a short's notional is its credit rather than the underlying it is on the hook for: 20 short AAPL 230 calls value at $3,700 against $457,000 of underlying. Fix: make the sign depend on whether the order OPENS exposure (intent plus the signed position already held for that leg's symbol), not on the leg side alone; and value a short option leg by margin or underlying rather than by premium. Pinned: tests/security/test_sweep7_adversarial_input.py::test_f30_the_exposure_change_of_an_opening_short_is_negative, ::test_f30_an_opening_short_must_still_be_measured_by_the_capital_checks (xfail(strict=False)), ::test_f30_the_same_checks_do_run_for_the_long_side. | mizan/risk/valuation.py:135-138,176-192; mizan/risk/checks.py:263,517-560,574-593,636-648,685-706,1219-1226,1395-1406,1489-1500,1803-1813 | OPEN

[2026-09-03T07:30:00Z] F-31 | HIGH | 7 | L1 (mizan/risk); the structural half is L0 (contracts, FROZEN) | **A named option strategy is never validated to BE that structure, so an unhedged short passes the whole decision plane.** `TradeProposal._structure` enforces a leg COUNT per strategy (`STRATEGY_LEG_COUNTS`) and nothing about sides, strikes or hedging, and no risk check implements Risk Canon R-OPT-3 - which `mizan/governor` quotes verbatim: "a spread that loses or unbalances a leg is a naked short". DEMONSTRATED against the default policy: (a) `strategy="custom"` with one SELL call leg is APPROVEd for 5 contracts; (b) `strategy="bull_call_spread"` whose two legs are BOTH sells is APPROVEd for 10; (c) `strategy="iron_condor"` of four identical short calls is APPROVEd for 20. The contract does refuse a `bull_call_spread` with a leg missing (leg count), so the sweep's literal target - "a spread with a missing leg" - is blocked; what is NOT blocked is the same position expressed as `custom`, or as a named spread whose legs are all on the short side. `restricted.strategies` can ban `custom` but cannot reach (b) or (c). The residual defence is the portfolio greek caps: `options_delta_limit`/`options_gamma_limit` do REJECT a 50-contract naked short, so exposure is bounded - but they are greek limits, not structure rules, and F-30 has already removed the capital checks that would otherwise size the position. Fix (L1, no contract change needed): a `structure_valid` check emitting the existing `STRUCTURE_INVALID` reason code, refusing any option proposal whose net short option exposure is not covered by a long leg of the same underlying and expiry, and refusing a named spread whose legs do not form it. The contract-level half - making `STRATEGY_LEG_COUNTS` a structure spec rather than a count - is a frozen-path change and needs an escalation. **NO SHIPPED POLICY STOPS IT.** `policies/institutional.yaml` - the strictest one this build ships: `max_short_gamma: 100`, `max_short_vega: 500`, `restricted.strategies: [custom]`, `require_invalidation`, `fail_closed.on_advisory_unavailable: true` - still APPROVEs a `bull_call_spread` of two short calls, a `bear_call_spread` of two short calls, and an `iron_condor` of four short calls, once the context carries the state those checks need and the advisory concurs. `restricted.strategies` reaches case (a) only; the short greek limits are portfolio-greek caps that pass at small sizes (they bind at 50 contracts, not at 5); nothing reaches (b) or (c). So this is not "the default policy is loose" - it is a missing control. Pinned: tests/security/test_sweep7_adversarial_input.py::test_f31_a_named_strategy_constrains_only_the_number_of_legs, ::test_f31_an_unhedged_short_option_structure_must_be_rejected[3 cases] (xfail(strict=False)), ::test_f31_not_even_the_institutional_policy_rejects_a_spread_made_only_of_shorts[3 cases] (xfail(strict=False)), ::test_f31_the_option_greek_caps_are_the_only_thing_binding_a_naked_short. | mizan/contracts/trade_proposal.py:47-59 (STRATEGY_LEG_COUNTS) and 176-205 (_structure); mizan/risk/checks.py:1838-1875 (no structure check in CHECK_FUNCTIONS); policies/institutional.yaml; policies/options-conservative.yaml (sets no short greek limit at all) | OPEN

[2026-09-03T07:30:00Z] F-32 | MEDIUM | 7 | L2a (mizan/advisory) | **The advisory pre-clamp erases the evidence that a compromised provider tried to exceed the deterministic cap.** `get_advisory._clamped` rewrites `recommended_quantity` down to the cap before returning, so by the time `governor.govern` sees the opinion `advised > cap` is never true and `ReasonCode.ADVISORY_CLAMPED` - which exists for exactly this - can never fire on the real pipeline. DEMONSTRATED: a provider returning REDUCE 99999 against a cap of 10 produces a recorded opinion of REDUCE 10 with empty reason codes; the string 99999 appears nowhere in the decision, and the record is byte-indistinguishable from a provider that honestly concurred at the cap. Calling the governor directly with the raw opinion DOES emit ADVISORY_CLAMPED, which is why the existing unit tests pass and the gap is invisible. The double clamp is correct and must stay ("one layer of defence is not a defence"); destroying the evidence is not, because a record that cannot show a provider misbehaving is the one thing you want when deciding whether a provider has been compromised. Fix: let `_clamped` carry the provider's own quantity through for a REDUCE above the cap (the governor clamps it and emits ADVISORY_CLAMPED), or keep the clamp and record the pre-clamp value. No invariant is affected: INV-01 asserts the AUTHORIZED quantity never exceeds the cap, which stays true under either fix. Pinned: tests/security/test_sweep7_adversarial_input.py::test_f32_the_governor_alone_still_flags_an_upsize_attempt, ::test_f32_an_upsize_attempt_must_be_visible_in_the_decision_record (xfail(strict=False)). | mizan/advisory/__init__.py:150-180 (`_clamped`); mizan/governor/__init__.py:137-141 (the unreachable branch) | OPEN

[2026-09-03T07:30:00Z] F-33 | MEDIUM | 6 | L3 (mizan/sdk) | **`@protected` with `ExecutionConfig(dry_run=False)` places a real broker order and only THEN raises `ConfigurationError`.** The refusal was added deliberately this sprint (recorded as an L3b DECISION in ledger/escalations.md) to stop `@protected` double-submitting, and it does stop the second order: the wrapped function is never called and exactly one order reaches the broker. But it is checked on `result.status == "SUBMITTED"`, i.e. AFTER the gate has already mutated the account. A developer whose error handler treats `ConfigurationError` as "the call was rejected, nothing happened" is wrong, and that is the natural reading of the class name. The condition is fully decidable before any work is done - `self.config.dry_run` is known at decoration time and at the first line of `guarded` - so the refusal can precede the evaluation, the ledger append and the mutation instead of following all three. Fix: raise from `Mizan.protected`, or at the top of `guarded`, when `not self.config.dry_run`. Pinned: tests/security/test_sweep6_gate_bypass.py::test_f33_protected_must_refuse_a_double_submit_config_before_it_submits (xfail(strict=False)), ::test_f33_the_caller_function_is_at_least_never_reached_on_the_double_submit_path. | mizan/sdk/__init__.py:224-232 | OPEN

[2026-09-03T07:30:00Z] F-34 | LOW | 6 | L3 (mizan/api) | **Anonymous `GET /v1/health` probes consume the authentication-attempt budget for the caller's address.** `health` calls `principal_of(request)` inside a `try/except MizanError`, and `principal_of` then `_refuse_credential` charges `auth_limiter` (keyed `auth:<client host>`) before refusing. Liveness probing is what that route is for, so a load balancer or uptime monitor polling it permanently exhausts the credential-guessing counter for everyone behind that address; after that every genuine bad credential from there answers 429 instead of 403, and the limiter can no longer distinguish a brute-force attempt from a health check. DEMONSTRATED with `auth_rate_limit=RateLimit(max_requests=3)`: five anonymous `/v1/health` calls, then a request with a bogus token returns 429. LOW rather than higher because it fails in the restrictive direction and a VALID credential is never throttled - a successful authentication consumes no budget (pinned). Fix: resolve the principal on `/v1/health` without charging the limiter, or charge only when an `Authorization` header was actually presented. Pinned: tests/security/test_sweep6_api_surface.py::test_f34_anonymous_health_probes_must_not_consume_the_auth_rate_limit (xfail(strict=False)), ::test_f34_a_valid_credential_is_never_throttled_by_someone_elses_failures. | mizan/api/__init__.py:346-361 (`health`), 189-215 (`principal_of` / `_refuse_credential`) | OPEN

[2026-09-03T07:30:00Z] F-35 | INFO | 6+7 | L5 (record) | **Negative results: what the new core defeated.** Recorded because a refusal nobody wrote down is a refusal that can be removed by accident. All of the following are pinned as PASSING tests in tests/security/ (250 new passing assertions across four modules). SWEEP 6: the only `submit_order` call sites in `mizan/` are `ExecutionGate._submit` and the one line inside `AlpacaPaperBroker.submit_order` that IS the mutation (AST-enumerated, not grepped); the `BrokerAdapter` Protocol has no cancel/replace/close vocabulary; `CHECK_ORDER` is the documented sequence with `kill_switch` second to last; execution disabled blocks before the broker is even read; an expired, not-yet-valid or already-consumed authorization blocks, and blocks on the REGISTRY alone when the broker has forgotten the order; two threads racing one authorization through one gate produce exactly one submission; a switch thrown during the TOCTOU re-read still stops the order, and `EnvKillSwitch` re-reads on every call with an unparseable value failing safe (F-4 not repeated); a forged upsized scope, an authorization presented with a different proposal, and an authorization used under another tenant's policy are all refused; a TTL outside 5..30s is refused at construction (F-11 not repeated); a REJECT decision cannot be authorized; the only environment variables the enforcement path reads are ALPACA_PAPER and MIZAN_KILL_SWITCH and no DEBUG/SKIP/BYPASS/FORCE/TEST variable exists anywhere in `mizan/`; `ExecutionConfig(paper=False)` is unrepresentable. STATE BINDING (the new sweep target): `authorization_hash` is a content hash and NOT a MAC - stated explicitly in a test so nobody mistakes it - and what actually holds is the gate's comparison against the decision and the proposal plus the TOCTOU re-evaluation; a bound state forged to all-zero hashes and response level 5 changes not one authorized unit; a policy swapped under the gate forces re-authorization; a response-level escalation between decision and execution forces re-authorization; a hand-edited authorization dies at USE, not only at construction; a tampered stored record raises `ChainIntegrityError` on read so `Mizan.execute` cannot act on it; SQLite refuses UPDATE and DELETE at full privilege. SWEEP 7: ten prompt-injection payloads (system-prompt override, XML verdict forgery, bidi override, JSON impersonating an advisory, SQL, NUL bytes, 19,999 characters) leave `proposal_id`, `evaluation_id`, `verdict_hash` and the authorized quantity byte-identical, and the governor module contains no reference to any free-text field; 20 adversarial advisory responses (APPROVE, APPROVE_MORE, INCREASE, claimed `authority_ceiling=override`, CONCUR-with-quantity, extra fields, truncated JSON, a bare string, a list, None, zero/negative/NaN/Infinity quantities, a JSON number, a duck-typed object, and providers raising RuntimeError/SystemExit/KeyboardInterrupt) all clamp to the deterministic cap or become unavailable, an advisory REJECT is always honoured, a provider exception message never reaches the record, and `fail_closed.on_advisory_unavailable` turns a provider failure into a REJECT; 34 schema abuses are refused by the contract (zero/negative/NaN/Infinity/exponent/JSON-number quantities and prices, duplicate leg indices, five legs, fullwidth and Cyrillic homoglyph symbols, zero-width joiners, newlines, path traversal, oversize reasoning, unknown fields, expiry ordering, equity/option leg mismatches). NO DETERMINISM BREAK WAS FOUND: identical hashes across repeated evaluation, dict insertion order, decimal spelling ("228.50" vs "228.5"), and across PYTHONHASHSEED 0/1/12345/99991, LC_ALL C/en_US/de_DE/tr_TR (the casefold trap), TZ=Pacific/Kiritimati and PYTHONUTF8=0; the wall clock is not an input to the verdict and replay is idempotent. API SURFACE: every /v1 route except health refuses an anonymous caller and a token without the scope, eight malformed credential shapes get one identical message, tokens are stored only as digests, another tenant's decision is NotFound and never Forbidden on read/execute/replay, a body naming another agent is 403 and a body choosing `proposal_id` is ignored, eight caller-supplied valuation/authority fields are all 422 (F-1/F-2/F-3 not repeated), no error body carries a traceback, path or vendor name, anonymous health returns liveness only (F-15 not repeated), all 11 security headers are on every response including 403/404/413, an oversized body is refused at 413 before the router runs, 20,000-deep nesting is refused without a traceback, and a wildcard CORS origin is refused at configuration time. CONSOLE: nine XSS payloads cannot emit markup through `render`, escaping never NFKC-normalises a homoglyph into real markup, `&` is escaped first, seven unsafe URL schemes become BLOCKED_URL, all 16 forbidden tags and every on*/style/srcdoc/background attribute raise, and bidi/zero-width controls are made printable rather than dropped. | tests/security/ | ACCEPTED

[2026-09-03T07:30:00Z] F-8 UPDATE | MEDIUM | 2 | L4 | CLOSED for the new core; legacy `app/` and `ui/` are unchanged and still disposable per C11. Re-verified this sweep at L4's request (ledger/requests.md REQ-15): `mizan/console/escaping.py` is the only sink, it has no unsafe-HTML parameter, `el()` refuses every executable tag and every event-handler/style attribute, URL attributes are filtered by `safe_url`, text is neutralised-then-escaped and never normalised, and `&` is escaped first so a double-encoded payload cannot be un-escaped. Independently pinned in tests/security/test_sweep6_api_surface.py (the Sweep 7.1 section) rather than only in tests/console, so the guarantee has a check outside the owning lane. | mizan/console/escaping.py | FIXED
